# Execution Plan — Subagent Breakdown (decision D9)

All work is delivered by Claude subagents orchestrated from the main session.
**Model policy:** `fable` = deep taste / visual sense · `opus` = medium-to-difficult engineering · `sonnet` = easy/mechanical.
**Testing:** every visual and performance test runs **on this machine** (the Mac serving `http://localhost:8137`) — decision D8.
**Motion:** designed by fable agents, then **reviewed live with Hannah** before it's locked.
**Guardrail for every agent:** never modify `golden-mushroom-page.html` or `mushroom-scene.js` — the running hero stays frozen; all build work happens in `glowshroom/journey-v6/` (dev copy) until final promotion.

## Wave 1 — launched immediately (parallel)

| ID | Task | Model | Output |
|---|---|---|---|
| W1-A | Baseline freeze: archive hero files, capture screenshots (all viewport modes + `?introat` intro points + callout states), measure bundle/TTI/frame-time, write BASELINE.md with regression budgets | opus | `BASELINE.md`, `archive/`, `baseline-captures/` |
| W1-B | Architecture ADRs (D2–D5) + dev scaffold: harvest map of `journey/` donor modules, world-layout ADR, `glowshroom/journey-v6/` scaffold that renders pixel-identical to the hero | opus | `adr/`, `glowshroom/journey-v6/` |
| W1-C | Anatomy & camera map (two pages, per `03`) drawn from the live hero's actual geometry + the approved stills | **fable** | `map/page1-anatomy.*`, `map/page2-camera.*` |
| W1-D | Placeholder content package: content module (anonymous contributors, PLACEHOLDER links, spotlight/card/profile stub copy per the locked copy table), no live modules | sonnet | `content/` |

## Wave 2 — after W1-B + W1-C (parallel)

| ID | Task | Model | Gate |
|---|---|---|---|
| W2-A | Spike A: extension lens — rear-cap geometry, orbit rough, plume treatment v1, dense Connect frame, optics stack + raw/finished toggle, budget table | **fable** | Review with Hannah |
| W2-B | Spike B: Owned portrait treatment at production density (placeholder faces), anonymous fallback node styling | **fable** | Review with Hannah |

## Wave 3 — after W2-A

| ID | Task | Model | Gate |
|---|---|---|---|
| W3-A | Grey-box prototype: full path Mission→Final, journey state, scroll scrub, thresholds, reverse-scroll, deep links | opus | — |
| W3-B | Motion pass on the grey-box: orbit spline, easing families, rest bands, text pinning, snap feel | **fable** | **Live motion review with Hannah on this machine** |

## Wave 4 — after W3 acceptance (parallel lanes)

| ID | Task | Model |
|---|---|---|
| W4-A | Chapter: Inspire (production) | **fable** |
| W4-B | Chapter: Connect (production) | **fable** |
| W4-C | Chapter: Owned (production, placeholder people) | **fable** |
| W4-D | Chapter: Final pullback (production) | **fable** |
| W4-E | Product systems: nav, routing, deep links, drawers/cards DOM, state torture-tests | opus |
| W4-F | Platforms: portrait poses wiring, touch model, bottom sheets, tiers + watchdog, reduced-motion static journey, Tier-3 capture script | opus |

## Wave 5 — hardening

| ID | Task | Model |
|---|---|---|
| W5-A | Full QA sweep vs `14-qa-acceptance.md` (25 criteria) on this machine, incl. emulated Tier 2 | opus |
| W5-B | Fix rounds (model chosen per defect class) | per-issue |
| W5-C | Decorative-motion cull pass per chapter (PS-4.4) | **fable** |
| W5-D | Content merge when real data arrives (replaces placeholders) | sonnet |
| W5-E | Final review with Hannah → promote `journey-v6/` to the live page | — |

## Status log

| When | Event |
|---|---|
| 2026-08-02 | Wave 1 launched (W1-A/B/C/D) |
| 2026-08-02 | **W1-D complete** (sonnet): `journey-v6/content/` — five-chapter content module with locked copy verbatim, 16 anonymous contributors (consent:false), 54 greppable PLACEHOLDER markers, deferred Equip content archived separately, COPY-TABLE.md maps every placeholder to its supplier. No fake stats or live modules. |
| 2026-08-02 | **W1-A complete** (opus): BASELINE.md + regression-check.md written; hero archived (both files, byte-identical, hero untouched). Numbers: 1.49 MB raw / ~319 KB gz over 14 requests (three.module.js = 87.5%); TTI 340 ms warm; locked 60fps with ~1.2 ms CPU frame cost; 41 draw calls / 44k lines / 24k points / 4,200 spores. Machine: MacBook Air M3, 8 GB. Findings: **hero has NO WebGL-failure fallback**; reduced-motion only stops the intro (scene keeps animating); 01 INSPIRE label clips at right edge in desktop mode (baseline, do-not-silently-fix); tab order and focus styling gaps recorded. Gaps: no persisted PNGs (browser-harness limit — Playwright pass recommended), no cold-cache TTI, no CPU-throttled Tier-2 numbers. **G0 pending: budgets are proposed, awaiting Hannah sign-off.** Screenshot-diff gate must be structural (anchors ±2px + renderer.info) — per-pixel always fails due to breeze/TAA. |
| 2026-08-02 | **Hotfix (orchestrator):** `journey-v6/index.html` input shield — OrbitControls wheel-zoom, drag-orbit, and tap-pulse blocked at capture phase on the journey page (hero page untouched); ambient cursor wind kept; `?free=1` escape hatch restores hero interactions. Verified live: 25 synthetic wheel events → zero camera movement with shield, large zoom with `?free=1`. Scroll-drives-journey arrives with W3-A (grey-box). |
| 2026-08-02 | **W1-C complete** (fable): both map plates in `map/` (naturalist-plate style, verified in browser). Key calls: orbit around the RIGHT flank ~160° ending ~20° off dead-rear (follows the breeze drift +x; reveals the gill-exposed lifted rim earliest); exits: ArtCompute = back-right (the plume already visible in the hero), Arca = rear-centre tallest, 2RP = back-left — mirrors to the approved still's layout from the rear pose; ADOS = the hero's existing hot core at the gill/stem apex; Hivemind braid doubles as the Connect→Owned exit guide; Owned glide heads −X toward the fairy-ring growth front. Conflicts logged (mockup nav, owned.png group names vs locked claims, inspire.png symmetry). Awaiting sign-off note. **WAVE 1 COMPLETE.** |
| 2026-08-02 | **Gate G0 CLOSED:** Hannah approved the regression budgets (BASELINE.md §8 now binding: ~bundle +10%, TTI +10%, +1 ms frame time at the hero pose; structural screenshot gate). |
| 2026-08-02 | **Wave 2 launched:** W2-A extension-lens spike (fable) → `journey-v6/spike-a/`; W2-B portrait-field spike (fable) → `journey-v6/spike-b/`. Both end in review sessions with Hannah. |
| 2026-08-02 | **W2-A complete** (fable): Spike A live at `localhost:8137/journey-v6/spike-a/`. Grade pass inserted into the hero's own composer (raw = the approved hero exactly; graded = +1 draw call only); 5,100 GPU spores born between real gill channels, rim curl, braided three-core plumes leaning with the breeze; orbit spline hero→Inspire rest with sequential exit reveal; dense Connect colonnade frame; anamorphic streak on one active exit. Budgets in BUDGETS.md — every state vsync-locked 60fps; hero-raw row exactly matches the frozen baseline. Self-flagged gaps: plume-core definition ~70% of the still, hero ambient shed competes with ArtCompute plume (production fix per ADR D3), Connect frame looser than the still, motion needs one visible-tab confirmation, reduced-motion grain freeze not wired. **Awaiting Hannah review (G2a).** |
| 2026-08-02 | **W3-A grey-box complete** (opus): full five-chapter scrollable journey live on `journey-v6/index.html`. Virtual scroll (24vh, ~24s traverse), PCHIP scroll→p (max gain step 2.1%), soft snap (measured settle), 0.45 p/s scrub cap; 31-point forward+reverse audit: 0 mismatches, never inside the stipe, nav=owned through Final, zero errors, 60fps at rests. Reused Spike A orbit; found+fixed 4 real bugs (T2 seam predicate needs floor clause — ADR amendment; flight-cancel camera disagreement; deep-link Back off-page; spike-a setLeanScale TypeError) + 2 motion defects (145°/s orbit whip → trapezoid; Final 180° whip-pan). Acceptance: all GB items pass except Final visually thin, Owned node reachability, label crowding, portrait poses formulaic — handed to W3-B/W4. GREYBOX-DECISIONS.md written. |
| 2026-08-02 | **Orchestrator:** spike-a setLeanScale guard applied (same unfinished-revision class as the labels/dust fix). |
| 2026-08-02 | **W3-B motion pass launched** (fable): handheld documentary layer (seeded, ?steady=1 QA), orbit breathe, Final rise-tilt-recede as one gesture + fog reveal timing, seam pass-through feel, copy fade choreography, reverse-scroll pacing, Inspire label stagger. Strict file boundary vs parallel W4-F. Ends with Hannah's ride-through script. |
| 2026-08-02 | **W4-F (first half) launched** (opus): `tools/capture.py` Tier-3 capture pipeline + complete static journey at `journey-v6/static/` + TIER-WIRING.md integration spec. New files only — no core conflicts. |
| 2026-08-02 | **GATE G3 APPROVED WITH NOTES (Hannah, live ride-through):** motion approved ("moving really nicely"; Owned + Final motion "feel really nice"). Binding note: eliminate no-man's-land — idle must always resolve to a chapter rest, forward-biased. Connect gills confirmed biologically accurate (anastomosing lamellae); production must read as blade-tissue not fabric. Wave 4 green-lit ("keep working through it"). |
| 2026-08-02 | **Wave 4 launched (batch 1 of 2, strict boundaries):** snap-commit scroll (fable — core/scroll.js+constants only); W4-D Final production (fable — chapters/final*, the empty stage: ring, cutaway, spore sky, sequential activation); W4-C Owned production (fable — chapters/owned*, Spike B treatment at the real leg, placement rules, ≥10 reachable nodes). Batch 2 after these land: W4-B Connect (blade-tissue gills), W4-A Inspire polish, grade unification across chapters, W4-E systems hardening. |
| 2026-08-02 | **Touch/bottom-sheets complete** (opus): input-ownership registry replaces selector guards (claimInput/releaseInput, modal mode kills travel keys); controls-first key dispatch by platform semantics; first-tap-arms/second-tap-opens per live pointerType; drag-dismiss sheets (28% or flick), journey p provably frozen during sheet interaction; 44px audit 21/21; closeCard fade restored; aria-expanded + live-region retarget announcements; footer internal scroll was dead — fixed via non-modal ownership-while-overflowing. Footer-peek report = hidden-pane vh-collapse artifact (hardened anyway). **Flagged for Hannah: mobile nav band moved to its own centred row (hero row overflowed 375px — Owned + CTAs were unreachable); needs design eyeball.** Chapter setSelected contract task launched (opus). |
| 2026-08-02 | **Spore unification complete** (fable): root cause — the T1 seam armed Inspire past the azimuth where the ArtCompute ramp had already saturated, so the plume system popped in fully formed. Now: per-exit azimuth arrival ramps beyond the arming angle; every plume particle starts AS a hero-shed member (own origin, hero's breeze law) and converts at hash-staggered moments onto its gill→rim→braid path; 9-capsule conservation dim (shed thins exactly as-and-where the braid condenses); byte-exact restore proven (checksums match pristine boot). One population evolving, forward and reverse. Residual taste knobs: far-downwind tail kept at ~50% dim (full erasure read as the wind dying); fast flights compress the handoff (continuous, converges at rest). |
| 2026-08-02 | **Snap velocity continuity complete** (fable): perceived-rate EMA (pVel, matched to SMOOTH_K so it tracks the on-screen rate through the idle window) seeds the glide; exponential blend to cruise (COMMIT_BLEND_K 6.0 replaces the from-zero ramp); target+bracket latched at engage; landing/settle behaviour structurally preserved. Traces: takeover velocity step 0.9% / 3.7% / 9.3% (bound 20%), backward resolution = one smooth zero crossing; 12/12 settle matrix; cancel same-frame. Feel dial: BLEND_K ~8 sharper / ~4 lazier. |
| 2026-08-02 | **Owned hover-only labels complete** (opus): generic `labelPolicy(nodeId)` contract (any chapter can adopt; unset = unchanged); contributors hidden at rest (verified 0 chips), hover/focus/touch-arm reveals "Name · Role" via the existing .hot union (parity structural); pods/Inspire/Connect byte-unchanged; aria-labels present while chips hidden; reduced-motion instant. Verified via its own headless Chrome (shared pane at tab cap). **Robustness note for polish round: a throw early in the frame loop (observed transiently from a mid-edit chapter) silently stops ui.update for everything downstream — core should isolate per-chapter animator errors.** |
| 2026-08-02 | **Grade unification complete** (fable): one lens p 0..1 with per-leg look curve (Connect halation 0.62 protecting edge polylines; Owned warm lift; Final +luminosity gain 1.14/hal 1.25); TAA NaN sanitise implemented and POISON-TESTED (16,384 injected NaNs healed in one frame); [g] raw everywhere; tier-2 identity verified; reduced-motion grain freeze wired. Residual polish list in BUDGETS (Inspire sheath, Final sky density, Owned deep-soil, visible-tab cadence). Only knob turned: SHEATH 0.72. |
| 2026-08-02 | **Agent pool exhausted mid-Final-declutter (credits reset 21:00). Orchestrator took over inline:** (a) found + fixed the remaining "new spore source" — ribbons/wisps faded in FULL-LENGTH (now draw on from the rim via uFade-keyed growth; beads hash-stagger in one by one; verified mid-transition clean, rest intact); (b) Owned portraits richer (desat 0.62→0.40, amber 0.90→0.72); (c) nav clicks = direct jumps (placeAt settle + straight camera blend w/ lift arc, cancel-on-input; verified Mission→Owned lands 0.725 exact, zero run-through); (d) in-scene pods REMOVED per Hannah — claims live once under the heading, hover fires the colony pulses via new chapter trigger(); labels/hotspots now contributors-only (16). Zero console errors across all verification. **Commit `bf871c7`.** |
| 2026-08-02 | **Ride-through #3 (Hannah) — spore note clarified: she was right, my first fix was incomplete.** The plumes ignited BESIDE the still-visible original curtain (kept ~50% outside the capsule dims) — two populations side by side on screen. Orchestrator fix: global hand-over channel in inspire-ambient (whole shed cedes to 0.97 as the furthest exit completes, smoothstepped, reversible); verified visually at rest (one system) and byte-exact restore at p=0 (7789.45663). Commit `437000f`. Connect hover note withdrawn by Hannah mid-sentence. **Connect-as-ground asked a second time — A/B grey-box variant offered.** |
| 2026-08-03 | **Dial + D17 re-axis complete** (fable): TRANSFORM T wires all 8 organization channels (default 0.30; [ ] keys, readout, ?t=, localStorage journey-v6.transform, query>storage>default, only interactive presses persist); braid/ribbon/wisp rises re-axised along BREEZE ratios — organization IN PLACE; belly clamp retired. **Locus proof: T=1 column 100% contained in T=0 drift envelope mid-orbit (centre shift ~1% frame), 91% at rest; no screen handoff through the orbit — "the bright column never moves while organizing."** Checksums byte-exact incl. mid-scrub T flips; zero errors. Commit `34625e4`. **Awaiting Hannah's ride + her chosen T.** |
| 2026-08-03 | **Ride-through #8 (Hannah): "it's a DIFFERENT stream — it SWITCHED." Diagnosis: spatial LOCUS JUMP** — the visible stream is a ~57°-leaned downwind diagonal (breeze 1.0,0.62,0.17) but the braid rises near-vertically from the rim lip (belly clamp damps lean further): the bright column RELOCATES during organization — same dots, different place = a switch to the eye. Fix folded into the running dial agent: re-axis the braid along the drift's own lean so organization happens IN PLACE (locus-overlap verification required, T=0 vs T=1 screen-bbox); belly clamp resolved in favour of locus (near-camera fade if anything streams at the lens). Dial (T, default 0.30, keys [ ], ?t=, localStorage) builds alongside. App-restart casualty earlier: first dial agent died pre-write, relaunched clean. |
| 2026-08-03 | **Spore unification complete** (fable): GPU layer DELETED from the live path (geometry+shader+Points; setTier no-op documented); pearls ported as per-dot brightness (exact math, separate RNG so approved assignments byte-identical); ~32% core cohort tightens braids; ribbons = only other geometry (restProx-gated); dots never dim at rest. A/B vs HEAD: the .248–.256 identity flip GONE. Checksum byte-exact ×3 traversal styles, zero errors. "Does the stream ever change identity?" — **No, by construction: no second particle system exists.** Rest slightly sparser/warmer — awaiting Hannah taste sign-off. Commit `ae1750e`. |
| 2026-08-03 | **Ride-through #7 (Hannah): the stream "switches to a completely different stream" in place — correctly identified the det crossfade as a SWAP** (hero dots → separate 5,100-dot GPU layer, different sizes/palette/cadence, co-located but different identity). Merge-doc §3 pulled forward: agent launched to DELETE the GPU spore layer from the live path — pearls ported to per-dot brightness modulation, ribbons stay (grown), dots carry the stream permanently, rest slightly sparser (sanctioned). |
| 2026-08-03 | **D16 restage complete** (fable): exits clustered AT the visible stream (AC=the stream az 5.83, Arca −31.5°, 2RP +24°; delta forks both ways); orbit ~90° right (was ~172° rear), stream in frame the whole leg; T1 at 48°; reveal sequencing chapter-owned; NO-SELF-IGNITION enforced (lip-first migrant lighting via uFrom, wisps trace the real walk, ribbons share uDet, gill band + cap-flow REMOVED as unfixable self-igniters); 22-point audit: Hannah's sentence answers NO everywhere; p=0 byte-exact; Connect entry re-keyed to follow the stream down; portrait retuned. Found pre-existing Back-flight stuck-camera bug (A/B proven) → orchestrator killed the flight path entirely: all route changes are direct jumps now (consistent with Hannah's nav rule); Back-to-Mission verified clean. Commits `04a7d21`, `c098e23` (merge doc). |
| 2026-08-03 | **SWARM VERDICT (3 parallel investigators, Hannah-requested):** delivery proven clean (45 files byte-identical; /journey/ URL-trap found → red OLD-BUILD banner added); census found 2 outright bugs — gill-band ramp straddled the arming azimuth (snapped on 20× early, wrong sector; moved to 96–118°) and faded hero callouts stayed hit-testable (cursor over invisible 01-INSPIRE flared the ceding curtain ~2× breathing — invisible to cursor-less automation; pointer-events now follow fades); runtime isolation MEASURED the core phantom — eff races 0→1 in ~0.02p so the eff-keyed det gate bloomed the 5,100-spore GPU layer while 2,000+ hero dots were still lit (double-exposure p 0.17–0.21, per-object luminance grid, no third source in a 62-object sweep). Fix: det also gated on rest proximity (pure in p, 0.235→0.253) — hero dots alone carry the braids through the whole orbit, detail sharpens co-located at the rest. Verified uDet=[0,0,0] @0.185 (one population), [1,1,1] @rest. Commits `0a78f0a`, `300e39f`. |
| 2026-08-03 | **Same-particle takeover complete** (fable): the hero's 4,200 shed dots ARE the transition — position takeover after the hero's integrator (render-order proven 12/12), CPU braid-path port incl. delta rim-walks, restore-by-ceasing via delta-tracked shadow (pop-free eased AND abrupt); GPU system demoted to rest-detail layer (dark until eff 0.85, co-located fade with brightness handback); per-particle brightness via the dimmer; worst-case 0.83ms CPU; checksum byte-exact both traversal styles. Honest answer to the acceptance question: during the whole transition NO new spore appears; final ~4° = co-located sharpening (one DET constant if Hannah reads it as arrival). Commit `7b536b8`. |
| 2026-08-03 | **Ride-through #6 — Hannah restated the spore note exactly; structural admission: every fix so far was still a CROSSFADE BETWEEN TWO PARTICLE SYSTEMS.** Definitive rebuild launched (fable): literal same-particle takeover — the journey overwrites the hero shed's own position buffer after the hero's animator each frame, steering THE ACTUAL DOTS onto the braid paths (hash-staggered conversion, rim-walk preserved); restore-by-ceasing (hero re-owns positions instantly); the chapter's GPU system joins only co-located at the rest for detail density. Acceptance question posed to the agent: "will she ever see a spore appear that wasn't already there?" |
| 2026-08-03 | **Ride-through #5 ("why still two sources") — orchestrator eyes-on diagnosis: the second population was the shed's DOWNWIND HISTORY** (old spores drifted far from the cap before the orbit) hanging as a detached cloud beside the growing braid — every prior fix treated the curtain as one thing; visually it's live-source + history. Fix: distance-graded hand-over in inspire-ambient (history beyond 1.2–2.6 cap radii dissolves with the FIRST arrival; near-rim live stream follows the weighted cede into the braid). Verified visually at p≈0.19 pre/post: one connected system. Restore byte-exact re-proven. **Also root-caused the review-sabotage: the :8137 server sent no cache headers → stale ES modules for Hannah AND agents all day; replaced with serve.py (Cache-Control: no-store) — refresh now always shows the latest build.** Commit `cecdf4a`. |
| 2026-08-02 | **Delta-split spore redesign complete** (fable): Arca/2RP spores are BORN in the source sector (new-spore appearance impossible by construction); rim-walk currents with beaded pulsing tips travel to their sectors and turn upward, braids draw on bottom-up on arrival; source curtain survives ~50% until the last current takes over (weighted global hand-over 0.50/0.28/0.22); persistent under-rim river links all three plumes at rest; reverse re-merges. Byte-exact restore re-proven. +2 draws, spores redistributed not added. Taste knobs: current fatness, pulse heat. Commit `1182363`. |
| 2026-08-02 | **Intro fast-forward complete (orchestrator, ride-through #4 item 2):** scroll during the entry choreography accelerates it — wall-clock skew ramps the scene grow-in through its own math in ~0.5s, CSS half compresses (callouts keep a quick 1-2-3 at 0.30/0.44/0.58s), journey boots immediately. Verified via same-origin iframe probe (harness round-trip latency made direct probing impossible — pages were ~10s old before samples landed). Commit `a12ddee`. |
| 2026-08-02 | **Ride-through #4 (Hannah), spore note #3 — structural diagnosis accepted: with three separate rim exits, two plumes are ALWAYS newcomers no matter how gently they arrive.** New approved direction per her "zoom in on the ones already visible and animate them interestingly": the DELTA SPLIT — the hero's single watched stream visibly divides, currents migrating along the rim to the Arca and 2RP sectors, everything traceably fed by the original; persistent faint rim currents keep the one-source truth legible at rest. Fable agent launched (arrival choreography only; approved rest look preserved). |
| 2026-08-02 | **Final declutter complete** (fable): root-cause diagnosis — additive rendering occludes nothing, so members were "glass lamps" (through-cap halo stacks) and the floor showed every underground stroke through the surface. Fixes: build-time elevation occlusion on members (the hero-shells equivalent); **opaque fog-colored soil slab** (colony now shows only in the cutaway, the still's exact reading); hero ground web scene-state dimmed w/ byte-exact restore; root flares → glow pools; surface strokes 520→140; two schematic far hints deleted; near members raised; spreads tightened; T2 density +60%; left rebalanced via content (director keys untouched); sky 3,600→5,200 spores, trees 48→26 half-tone; frontWorld() halation hint API. Draws net −1, overdraw down, zero errors. Verdicts: both complaint rounds pass. **Flag for Hannah: Owned portrait faces now clearly legible in the epilogue's cutaway wedge — keep or suppress?** |
| 2026-08-02 | ~~**Final declutter relaunched** (fable, credits back):~~ superseded by completion above | forest-floor line noise = priority 1 (her round-2 note), species-unity diagnosis required before changes, rebalance, sky density, growth-front hint API. |
| 2026-08-02 | ~~PENDING when credits reset (21:00): relaunch Final declutter~~ **done, see above** (sticks/imbalance/species-unity/sky-density — full brief in the killed agent's launch prompt, reusable). **OPEN QUESTION for Hannah/Peter: Connect re-sited to the ground surface? Orchestrator recommended keeping the under-cap chamber (see chat); parked, not actioned.** |
| 2026-08-02 | **Ride-through review #2 (Hannah):** Mission "still feels very nice" ✓. Notes → actions: (1) ONE spore source — the hero's original right-drifting spores must BECOME the Inspire plumes, no second population (fable agent launched, chapters/inspire*); (2) snap glide must continue smoothly from the user's motion — velocity-continuous handoff, no stop-restart (fable agent launched, scroll.js); (3) Owned contributor tags hidden until hover/focus, then name+role together (opus agent launched, ui flag + owned registration); (4) Final feels cluttered/imbalanced — messy "sticks" on the right, left imbalanced, hero still reads as a different species from the members; declutter and simplify (**QUEUED behind the running grade agent — same files**). |
| 2026-08-02 | **W4-B Connect production complete** (fable, longest build of the day): blade-tissue verdict pass — silk killed by monotonic edge profiles + fine serration + light moved to 1-px free-edge polylines + dark sector behind copy; three behaviours as distinct verbs (ignites-in-place / converges-then-echoes / travels-and-remembers), never one pulsing object; Community re-sited to the rest gaze (W3-B compromise resolved); all three anchors verified in landscape + both portrait sizes (Hivemind chip orientation-aware); hero bokeh dimmed→retired with exact restore; camera-driven reversible exit. **Critical discovery: two NaN-blackout mechanisms on ANGLE/Metal (pow interpolation-negative base; command-buffer aborts) — TAA holds NaN forever; fixed locally + reusable NaN soak; one-line core TAA sanitise handed to grade pass.** 51 draws, 60fps locked, zero errors. **ALL FIVE CHAPTERS AT PRODUCTION QUALITY.** |
| 2026-08-02 | **Commit `72386aa`** (local): full production batch. Push still blocked on dead token. |
| 2026-08-02 | **Grade unification launched** (fable, sole agent): extend the G2a lens across p 0..1, reconcile every chapter's documented notes (Owned pre-compensation, Final no-double-fog + luminosity lift, Connect edge halation, Inspire sheath width), implement core TAA NaN sanitise, reduced-motion grain freeze, tier-2 identity check, [g] everywhere. |
| 2026-08-02 | **D15 ring hero-density rebuild complete** (fable): 825 → 21,280 line segs + 2,001 pts across the ring (near members ~24% of hero build each, mirrored technique: cap lattice+overlay net+motes, feathered gill fans w/ cavity curve, 3-ring rim + front arc, stem lattice+fibres, forking ground-merge roots; far-side damping replaces occlusion shells at zero draws); per-system opacity via build-time color mul in the shared batch — **draw calls identical (104), frame cost unchanged (vsync-locked)**. Same-render verdict: pass — one material world at rest and mid-reveal; characters preserved; whisper state carries density. Tier-2 cut order recorded. (Nav showing OWNED at epilogue = designed behaviour per handoff, not a bug.) |
| 2026-08-02 | **setSelected contract complete** (opus): owned.js routes contributors via the bridge's exact mapping (guarded release) + pods get a held sel channel (0.55, no claim pulse); inspire.js holds the selected exit's coherence + streak priority hover→selection→auto, alias-safe; bridge retirement proven empirically (stub test); 10/10 verification paths green. Connect owes the method (noted §24); Final exempt by design. Duplicate :8137 server the agent spawned was killed by orchestrator; original intact. |
| 2026-08-02 | **Hannah review of ring: NOT same-render — "way worse texture."** Root cause: D14 carried form at ~1/50th hero density (825 segs total vs hero's 44k). **Hero-density rebuild launched** (fable): near members 30–40% hero density each, mid 15–20%, far 8%, texture systems replicated (cap lattice+points, dense gill skirt, stem fibres, ground-merge, glints); batching kept; frame-cost measurement required. |
| 2026-08-02 | **D14 ring variants complete** (fable): ring members rebuilt as parameterized instances of the hero's actual form recipe (rim harmonics, droop+fold, rolled margin, cap lumps, soft-bell dome, gill cavity curve w/ vein gills, stem taper/ground-merge flare, heat palette) with seeded per-member individuation — nine distinct characters (patriarch m1, broad-brimmed elder m5, button m8 closing the ring beside the hero, etc.), per-member shed strength weighting the sky sources with zero sky edits. Draw calls unchanged. Same-species verdict: genuine; caveat: two near short members read ~15% more open from above until the grade adds density. Undarken/reverse/primordia intact; hero at p=0 pixel-identical. |
| 2026-08-02 | **Portrait framing complete** (fable): authored per-rest offset fields (dolly/rise/truck/re-aim/fov) smoothstep-blended over poseAt by aspect (zero at ≥1, full at ≤0.75), guard keys pin the field near zero in the tight slip-under/descent clearances; poseAt stays pure with aspect as a param; landscape bit-identical (20-sample 10-decimal diff clean); all five portrait rests verified at 430×932 + 375×812 vs map thumbnails; ?aspect=portrait QA. Flagged: hivemind anchor unholdable in portrait (relayed to Connect agent); footer cue peeks at 375×812 Final rest (relayed to touch agent). |
| 2026-08-02 | **W4-E footer+a11y complete** (opus): progress-driven footer (reveal band 0.955–0.992, above the commit point so it's never half-parked; SITE INFORMATION cue flies to p=1 and hands focus); roving tab order finished incl. two invisible-but-focusable leaks fixed (.j-nav at hero, callout tags off-chapter); nav is a labelled landmark; cards are true aria-modal dialogs with trap+return; focus styling = hover parity; Space-key conflict guarded (root cause in scroll.js catalogued); selection hook added (openCard/closeCard → chapter setSelected, bridged to owned-portraits' orphaned implementation). One introduced regression (bare nav{} rule leak) caught and fixed. Six-item pre-existing a11y debt list catalogued. |
| 2026-08-02 | **Touch/bottom-sheet agent launched** (opus): first-tap-hover/second-tap-open, bottom sheets w/ drag dismiss + working internal scroll, 44px targets, a11y debt items 1/4/5 root-caused. Boundary: ui*, scroll.js input routing only, index.html journey block. |
| 2026-08-02 | **W4-D Final production complete** (fable): D12 implemented — ring centred at (−6,−0.8) so the hero sits ON the arc (member, not parent, ~11° right of centre at rest); undarken reveal = monotonic camera-driven kindling CCW from the hero, reverse re-darkens; camera re-keyed within the approved gesture family (yaw 833–970°/p, single pitch crest, arrival=recede vector); growth-front pulse 13–18s revolutions; primordia on ≥6s dwell; CTA hooks wired (incl. footer delegation). Zero errors; narrow-window verified. Honest gaps: p≈0.85 quietest frame; raw render less dense/luminous than the still (grade's job); hero ground network needs a clipping plane (logged). Budgets recorded; grade-pass notes: manual fog uniforms (don't double-fog), ring halo/core knobs. |
| 2026-08-02 | **Portrait-pose agent launched** (fable): authored per-chapter portrait compositions vs map thumbnails, aspect-blended offset field over poseAt, landscape bit-identical, ?aspect=portrait QA. Boundary: director.js integration + new core/portrait.js. |
| 2026-08-02 | **W4-A Inspire polish complete** (fable): core cohort + travelling knot pearls (Arca hottest per map) + 9 shader-driven core ribbons (+1 draw) bring plume definition to full vs the still (open taste knob: sheath width, flagged for grade pass); ArtCompute unswallowed via corridor-dim of the hero shed (restored verbatim on exit, verified numerically); streak auto-follows the igniting exit, Arca at rest; hover trace-back (plume→rim→gill origin) implemented on all three. Hero p=0 regression clean. |
| 2026-08-02 | **W4-C Owned production complete** (fable): Spike B treatment ported against the REAL leg (107-point polyline sampling); 48-node photo field, 7,400 hyphae + 6 cords + soil-underside lid so both crossings pass through material; pods + claim pulses; consent enforcement in code (48/48 forced anonymous with today's consent:false content); reachability fixed (13 hotspots live at rest, 16/16 contributors framed); 60fps, zero errors, Mission byte-clean. ACES compensation constants recorded for grade unification. **Flagged: parallel connect.js on disk has a GLSL reserved-word (`patch`) compile failure that blacks the composer when Connect arms — relayed to W4-B agent urgently. Also: ui.js lacks a selection hook (relayed to W4-E agent).** |
| 2026-08-02 | **Commit `5272da7`** (133 files) — local only: GitHub token on disk is DEAD (401) and no other credential exists on this machine (no gh, no keychain entry, no SSH keys). Push blocked until Hannah/Peter mint a fresh classic PAT (repo scope) into `~/.github_token`. Committing locally at each milestone meanwhile. |
| 2026-08-02 | **Snap-commit complete** (fable): no-man's-land eliminated — idle always resolves to a rest; COMMIT_THRESHOLD 0.35 (forward-biased), glide 0.10 p/s riding the normal scroll pipeline (copy/seams behave identically); p=1.0 added as terminal anchor; 12/12 settle tests + cancel-mid-glide + 30k px fling verified, zero errors. Feel dials for Hannah's next ride: threshold→0.30 for harder push, glide→0.12 if leisurely. `?nosnap=1` QA escape. |
| 2026-08-02 | **Orchestrator:** work snapshot tarball at `glowshroom/archive/snapshots/` (protection — the spike-a near-loss showed uncommitted work has no restore point; recommend a git commit when Hannah says go). |
| 2026-08-02 | **D12 relayed to W4-D mid-build:** hero joins the fairy ring; undarken reveal; scoped director.js Final-leg exception granted. **Batch 2 launched under D13 (keep moving always):** W4-B Connect production (fable — blade-tissue gills, three distinct behaviours, resolve community-vs-copy composition) + W4-A Inspire polish (fable — plume cores to full definition, unswallow ArtCompute, hover trace-back). Five agents now in flight, boundaries: scroll.js+constants | final*+director-final-keys | owned* | connect* | inspire*+anatomy. Remaining queue: grade unification, W4-E systems, mobile portrait poses, tier wiring (needs D11). |
| 2026-08-02 | **W3-B motion pass complete** (fable): handheld documentary layer (seeded, zero at rests, fades under fast scrub, ?steady=1 kills it); orbit breathe (±9% ease variation, azimuth still strictly monotone); Final re-keyed into one rise-tilt-recede gesture (yaw peak 1665→964°/p, fog staggered so the sky breathes open at the soil crest); T2/T3 seam fog-dips (pass-through feel, C1, closed before rests); copy release-on-travel / re-anchor-on-settle choreography; reverse byte-identical; Inspire labels moved to plume bodies with 150ms stagger + suppression hysteresis. Ride-through script delivered. Known Wave-4 items: Final stage nearly empty, optics stop at p 0.38, dips are fog-only stand-ins, handheld amplitude deliberately conservative (Hannah's knob). **BUILD FROZEN for Hannah's G3 ride-through — no agents touch journey-v6/ until her verdict.** |
| 2026-08-02 | **W4-F (first half) complete** (opus): `tools/capture.py` (CDP over stdlib WebSocket — ADR D5's --screenshot form hangs on this page; DPR 1 with measured --quantize option) captured all 10 stills clean (5 poses × desktop+mobile, 11.6 MB, none need the freeze to *look* right — freeze buys reproducibility); complete Tier-3 static journey at `journey-v6/static/` — zero canvas, zero errors, all content as authored HTML with `data-src` drift-guard (117 strings in sync), works with scripting disabled, AAA contrast over the stills, reduced-motion honoured, routes + focus management verified at both sizes. TIER-WIRING.md spec for the integrator (capability doors, ?capture= freeze list incl. captureReady flag, watchdog rules, gate hook). **Raised decision D11:** routing prefers-reduced-motion to static changes the accepted Mission path for those visitors — needs explicit ack. |
| 2026-08-02 | **Spike B rev 2 complete** (fable) — **G2b condition met, gate fully closed.** No-empty-frames audit passed at 19 scrub positions across two aspect ratios (fixed one real violation: node <1.5 units from path at u≈0.4 — now a 3.0-unit clearance rule); full Owned chrome as real DOM asserted from content.js (heading, primary+secondary pods with distinct colony-pulse hovers, wordmark, nav w/ Owned active, 2RP/Discord pair); camera reworked to a lateral arc (view 45–75° off motion vector, nothing streams at the lens) with frame-cell stratified node placement; boots into photos×48. 60–63fps, zero errors. Placement rules copied into 09-chapter-owned.md. Taste note for Hannah: the moving camera lives "inside the colony" so faces run larger than the still's wide vista — `?u=` scrubs for judging. |
| 2026-08-02 | **Gates G2a + G2b APPROVED (Hannah):** blanket approval — Spike A v1 is the lens baseline; Spike B approved conditional on rev 2 landing clean. Recorded in DECISIONS.md. |
| 2026-08-02 | **Wave 3 launched: W3-A grey-box prototype** (opus) — full scrollable five-chapter journey in `journey-v6/`: scroll model + soft rests, complete camera path with the four seams + fog re-parameterisation at rise, DOM copy layer + nav + flights, hash routes/deep links, interaction skeleton, hero preservation. Reuses Spike A's orbit/plumes for Mission→Inspire. W3-B motion pass follows; Hannah's live ride-through after that. |
| 2026-08-02 | **Spike A repaired (orchestrator):** the stopped v2 agent had left `director.js` expecting unbuilt `labels`/`dust` modules — every key handler threw. Fixed with inert defaults; verified live: all poses, key switching, and orbit playback work (orbit stall in headless test = known hidden-tab rAF artifact, advances when frames pump). Spike A is v1 behaviour, as Hannah directed. |
| 2026-08-02 | **Spike B rev 1 complete** (fable): [p] photo mode + [d] 48-node density live. Verdict: **photos beat the busts** — 48-node photo frame is closest yet to the approved still. Production findings: photos need ~2× colour push (62% desat + 0.90 amber multiply), background edge-burn is make-or-break (narrow working window → per-photo QA needed in production), wider mask feather; rim language unchanged. 48 nodes: 60fps, same draw-call count, voids intact; noted "row of coins" chains → size/spacing jitter rule for OW-4. |
| 2026-08-02 | **Spike B rev 2 launched** (fable, fresh agent — resume unavailable): Hannah's notes — no empty frames anywhere on the path (incl. vertical gaps), full Owned page copy/chrome (heading, claim pods w/ hover pulses, wordmark, nav w/ Owned active, 2RP/Discord pair), arc camera sweeping past faces (not head-on), decentralized composition, boot state = photos+48. |
| 2026-08-02 | **W2-B complete** (fable): Spike B live at `localhost:8137/journey-v6/spike-b/`. 9,900 flow-field hyphae + 5 rhizomorph cords, 16 portrait nodes (procedural busts) with 87 strand curves terminating exactly at nodes, real 3D rim fibres, frayed masks, defocus/haze depth; anonymous spore-print fallback; donor grade + raw toggle. 60fps sustained, 0.3–0.8ms CPU, zero errors; budgets appended. Self-verdict: embedded-not-pasted **passes at spike level**; caveats: painterly busts need re-tuning against real photos, 16 &lt; "dozens" density, close-hover runs hot, and a composer finding (runs WITHOUT OutputPass; sRGB-encoding olive-washes the additive field — must be reconciled with the hero composer at merge, flagged in BUDGETS.md). Keys: space glide, g raw, a anonymous, b HUD. |
| 2026-08-02 | **Spike B photo revision launched** (same fable agent, resumed): [p] procedural→photo→anonymous cycle using the staged test portraits; [d] 48-node production-density test; report on what real photos need to sit in the field. |
| 2026-08-02 | **Hannah direction for Spike B:** test with REAL photos, not only procedural placeholders. 26 free-license test portraits downloaded to `journey-v6/assets/test-portraits/` (20× 128px randomuser.me + 6× 512px pravatar.cc, README marks LOOK-DEV ONLY / delete before launch; consent rule unchanged). Photo-mode revision queued for when the running Spike B agent completes. |
| 2026-08-02 | ~~G2a review #1~~ **CORRECTION (Hannah): the "zooming at me / arc / sparse" review was about SPIKE B (the Owned field, watched mid-build in the shared browser pane), NOT Spike A.** Spike A is to be left exactly as delivered; the mistakenly-launched Spike A v2 agent was stopped mid-edit and Spike A verified still loading clean (zero console errors). Spike A's G2a review by Hannah has NOT happened yet. Her Spike B notes to fold into the next B revision: camera should move as an ARC (not things coming straight at the viewer), and the field must be less sparse / less centralized — distribute faces and structure through the whole volume instead of clustering in the centre. Queued behind the running photo/density revision. |
| 2026-08-02 | **Hotfix 2 (orchestrator):** tap/click restored on `journey-v6` per Hannah — the shield now replays a quick same-spot down+up through to the canvas (tap pulse works; zero drag delta so the camera can't move), while drag-orbit and wheel-zoom stay blocked. Verified via event trace: replayed pair reaches the canvas; drag and wheel storms leave the camera pixel-identical. |
| 2026-08-02 | **W1-B complete** (opus): six ADRs in `adr/` (D2–D5 + AR-6 resolved in DECISIONS.md); `journey-v6/` scaffold live at `localhost:8137/journey-v6/index.html` — console-clean, parity with the hero within run-to-run temporal noise (MAE 1.69–3.34 vs same-URL noise 2.79). Key findings: the hero needs **zero modification** (its `createScene()` already exposes `groups`/`consts`/`addAnimator`; extensions parent to `groups.mushroom` and inherit cap bend/sway); four occlusion seams replace veils; the hero's `Fog(bg, 7, 20)` clips past ~20 units → **the rise seam must re-parameterise fog before the Final pullback** (hard constraint on chapter 10); donor `equip.js` moved to `journey/deferred/`. |
| 2026-08-03 | **M1 promote executed** (15-merge-and-architecture.md §4): `journey-v6/index.html` is now `glowshroom/index.html` — the site serves the journey at `/`. Mechanical move, no behaviour change. **Moves:** old pre-journey Banodoco page `index.html` → `archive/index-presite.html`; `journey-v6/index.html` → `index.html` (root); `golden-mushroom-page.html` → `archive/golden-mushroom-page.html` (byte-identical, sha256 `5638dd0d…` matches pre-move — this archived copy is now intentionally non-runnable; the last runnable copy is tag `v6-prepromote`); old `journey/` build → `archive/journey/` (red OLD REFERENCE BUILD banner untouched). **Redirect stubs** created at all three vacated paths (`golden-mushroom-page.html`, `journey-v6/index.html`, `journey/index.html`), each a tiny `<meta http-equiv="refresh" content="0; url=/">` page with a fallback link. **Path-rebase inventory:** 6 rewrites in the promoted `index.html` (2 importmap entries `../vendor/...` → `./vendor/...`; `../mushroom-scene.js` → `./mushroom-scene.js`; `./journey.js` dynamic import → `./journey-v6/journey.js`; 2 stale comments mentioning `../golden-mushroom-page.html`/directory depth rewritten for accuracy — no functional link to that file existed in the markup, only comments). Module-relative `import` statements inside `journey-v6/**/*.js` were left untouched (correctly resolve against the module, not the page). **Document-base-relative runtime path found and fixed:** `journey-v6/core/ui-footer.js` set `staticLink.href = 'static/'`, which resolved correctly from `journey-v6/index.html` but would 404 as `/static/` from the promoted root — fixed to `new URL('../static/', import.meta.url).href` (chosen style per the plan: import.meta.url, survives future directory renames). `chapters/owned-portraits.js` already used `new URL(TEST_PORTRAITS.base, import.meta.url)` for the look-dev test photos — already page-move-safe, untouched. Also fixed a stale in-page back-link in `journey-v6/static/index.html` footer (`../index.html` → `/`, since the old target now only exists as a redirect stub) and the `capture.py` doc comment describing the `?capture=` grep target. **Tooling:** `journey-v6/tools/capture.py` `BASE_URL` → `http://localhost:8137/index.html`; `journey-v6/static/captures/manifest.json`'s `source` field left as-is (historical record of an actual 2026-08-02 capture, not live code). **Verification (server already running on :8137, no-store headers):** `/` → 200, serves the journey page (title/markup/importmap all confirm, not the old Banodoco page); all rebased refs curl 200 (`vendor/three/three.module.js`, both `vendor/three/addons/...` samples, `mushroom-scene.js`, `journey-v6/journey.js`, 8 more `journey-v6/core|chapters|content` modules, `journey-v6/constants.js`); all three stubs curl and contain the redirect meta tag; the `ui-footer.js` fix verified two ways — curl `/journey-v6/static/` → 200 and `/static/` → 404 (proving the old bare path would have broken), then live in-browser: `document.querySelector('.j-foot-note a').href` resolved to `http://localhost:8137/journey-v6/static/` exactly. Browser ride-through confirmed: hero loads clean (zero console errors), journey boots (`window.journey`/`window.sceneApi` present), `journey.scrollTo(1.0)` reaches the Final chapter with nav, footer, and links all rendering correctly, zero console errors throughout. Landed as one commit (M1 promote), not pushed. |
| 2026-08-03 | **M2 organism extraction executed** (15-merge-and-architecture.md §4): `mushroom-scene.js` (2,095 lines, one closure) mechanically split into `organism/` — **zero behaviour change, proven**. **Modules:** `organism/organism.js` (1,803 lines — engine, builders, cap/shells/gills/stem/ground/motes geometry, breeze, tap, frame loop, setView, public API, and the assembly: extracted modules are CALLED at the exact positions their inline blocks held, so the deterministic RNG stream and animator registration order are bit-identical); `organism/spores.js` (201 — §10 shed creation, the `spore-drift` integrator + mouse wind, `shedSpores`, and the **M3 driver-seat stub**: `driver: null` + `setDriver()`, read by nothing yet — inert by construction); `organism/intro.js` (166 — draw-window keying, convergeDraw/riseDraw, shell fades + stem clip, `?introat`, the `intro-draw` animator); `organism/furniture.js` (76 — region highlights §11 incl. base-opacity capture at the original position, tracker projection). Shared closure state travels via one `ctx` object; mutable per-frame sway state (`ctx.swayCos/swaySin`) written by 'breeze', read by spores — same frame. **Ordering constraints preserved with comments:** 'breeze' → 'spore-drift' → 'highlights' → … → 'trackers' → 'intro-draw' registration order (journey animators still land after the hero's, so the takeover keeps overwriting spore positions AFTER the hero integrator — the proven-load-bearing relationship); highlights' base opacities captured before any animator runs; trackers read the matrix 'breeze' updates. **Compatibility surface (unchanged):** sole export `createScene()`; returned api = `scene, camera, renderer, composer, controls, groups{mushroom,stem,sway,ground,spores}, consts{CAP_Y,CAP_R,CAP_H,STEM_TOP,FOG_NEAR,FOG_FAR}, setHighlight, setView, addAnimator` — verified against a full grep census of every sceneApi access in journey-v6/ (23× groups, 12× scene, 11× camera, 10× addAnimator, plus composer/renderer/controls via destructuring). One ADDITIVE key: `spores` (the system handle carrying the M3 seat), unused by any existing code. **Import repoints:** `index.html` → `./organism/organism.js` (the one site the plan named), plus two more found by grep and repointed: `golden-mushroom.html` (old dev page) and `journey-v6/spike-a/main.js` — both verified loading clean. Three stale index.html comments referencing mushroom-scene.js rewritten for accuracy (M1 precedent). **Archive:** `git mv mushroom-scene.js archive/mushroom-scene.js`, sha256 `eee1967d…f87d7e` = BASELINE BF-1 exactly (byte-identical); no shim at the old path — grep proves zero live imports remain (runnable pre-split state = tag `v6-prepromote`). **Gates:** (a) p=0 shed color checksum **7789.45663** exact at boot AND after a 0→0.26→0.925→0 round trip (index-weighted i%1000 sum 3824844.72915 also exact vs pre-split probe); (b) structural counts pre=post identical: 42 draw calls, 12,829 tris, 24,090 points, 44,377 lines, 25 geometries, 15 textures, 22 programs (like-for-like intro load; a nointro load compiles 2 fewer clip-plane shell variants — path-dependent, both builds), 4,200 spores, 6 scene children; (c) browser: zero console errors across `/`, `/?nosnap=1`, `?introat=0.55`, golden-mushroom.html, spike-a; intro plays correctly (mid-intro + frozen 0.55 match BASELINE §5 states 6–7); journey verified at p=0 (hero + callouts), 0.26 Inspire rest (braid takeover live — the fragile reach-into-spores path), 0.925 Final; setHighlight boost ×2.12 mid-breathing-range per BASELINE §5.2; (d) perceptual: capture.py mission@1440×900 pre-vs-post MAE 4.50 / 8.06% ch>8 — BELOW the same-build back-to-back noise floor (MAE 5.10 / 9.42%), i.e. the split is indistinguishable from run-to-run noise. Landed as one commit (split + archive move), not pushed. |
| 2026-08-04 | **M4 chapter foldering + route manifest executed** (15-merge-and-architecture.md §4, four commits, each landing green: `7c40428` promote, `d77d3d6` route.js, `2d815e2` composer, `8b69bf2` foldering). **Final tree:** `glowshroom/{index.html, serve.py, organism/, journey/, content/, static/, tools/, assets/, archive/}`; `journey/` = `journey.js` (the spine) + `route.js` + `{state,scroll,director,seams,lens,ui,ui-footer,portrait,anatomy,constants}.js` (core/ flattened per §2; journeyState→state) + `lib/helpers.js` (kept live — owned* uses it) + `chapters/{inspire,connect,owned,final}/` each `{index.js, camera.js, [anatomy.js], …}` (multi-file chapters folded: connect/{blades,structures}, owned/{leg,portraits,substrate}, final/{ring,sky,terrain,world}); spikes + GREYBOX-DECISIONS.md → `archive/` (spike-b imports repointed so the archived pages still load); redirect stubs kept at `/journey-v6/index.html`, `/journey/index.html` (journey/ holds modules, no page), `/golden-mushroom-page.html` — all curl-verified. **route.js shape:** `ROUTE = [{id, span (relative integer units, today percent), nav, stops (chapter-local rest fractions; first = canonical rest), scrollVh}]`; derived: `CHAPTERS` (start/end via cumulative span/total), `REST_STOPS` (snap anchors + handheld zeros), `CHAPTER_IDS` (hash routes), `restProgress/startOf/endOf/chapterAt/navChapterAt`, `TERMINAL_P`; module-init assert recomputes vs the shipped legacy table (tol 1e-12, console.error on drift). **Derived-number proof:** integer/100 divisions land on the exact shipped doubles — all boundaries, all rests (0.26/0.49/0.725/0.925), seam-dip centres (0.436/0.693), copy bands, fog farFromP/nearFromP bit-equal to the retired literals; 1-ulp-only diffs at fog farToP/nearToP, lens key 0.68, restProx ramp start 0.235, FOOTER_LO (all feed smooth ramps — sub-noise). Everything previously hardcoding a chapter-range number in core/+journey.js (scroll allocations, RESOLVE_P, ORBIT_P1, HH anchors, seams' T1 relax + hold windows + T4 minP, lens LOOK_KEYS, footer band, focus handoffs, Inspire retire/restProx ramps, nav-active fallback) now reads route lookups. **Composer:** chapters/<id>/camera.js owns each leg's keys in LEG-LOCAL t (global p preserved in comments); inspire/camera.js also owns the Mission→Inspire ARRIVAL gesture (gesture-local u, dead band 0.040/0.26 as a fraction); director.js concatenates legs per route (p = start + t·span — round-trips to the same double for all 25 keys, verified), computes tangents globally, and documents the FROZEN order: base leg → chapter modifiers (none shipped) → global language (portrait field, handheld, fog dips + T4 ramp, no-roll) → lens. **driveInspire migrated** into chapters/inspire/index.js as contract method `drive(p)`; journey.js runs generic per-chapter drive/snap loops and keeps only boot/frame-loop/route+scroll wiring/direct jumps (THREE import gone). **Anatomy dedupe:** EXITS → chapters/inspire/anatomy.js (ONE file; organism reads only what the seat passes — M3 seam intact); final/ring.js H_CAP_* now read the shared mirror; ring REST_CAM + the Final focus fallback (migrated from journey.js as `focusWorld()`) now read the chapter's own 'final-rest' camera key — the stale-mirror bug class is gone. **Gates:** (a) 61-point scrub audit (31 stops fwd+31 back, ?nosnap=1&steady=1): all core columns (chapter/nav/armed/copy/hash/pose/fov/fog) byte-identical to the pre-M4 baseline at every stop and every step; two borderline hotspot chips (ados @0.49–0.53, contributor-15 @0.78, each ~5–10 px inside the copy-rect suppression hysteresis band) flap between same-session re-runs by pre-existing design (inputs proven bit-identical). (b) camera parity: poseAt 201-sample dense comparison (12 sig digits, landscape + portrait) maxAbsDiff **0** after every step; capture.py stills vs pre-M4 within the same-session noise floor (mission 4.34 vs floor 4.65; inspire 7.75 vs 8.78; final 4.98 vs pre-pair spread 4.81/2.91). (c) p=0 checksum **7789.45663** (+ 3824844.72915) exact after every round trip incl. a framed Inspire-rest visit (seat active, 4,200 dots) and a plain-load full ride. (d) **extension drills** (scratch branch, discarded): drill 1 — one key edit in inspire/camera.js (pos z + fov of 'inspire-rest-drift'): ONLY that file dirty; effect verified live at p 0.312, rest + hero pose untouched. Drill 2 — second connect stop declared as `stops: [0.5, 0.8]` in route.js: ONLY route.js dirty; REST_STOPS gained 0.556, snap-commit resolved to it from both sides (commitP 0.49/0.556/0.556 at p 0.51/0.545/0.58), nav landing unchanged, zero scroll/ui edits, zero console errors. (e) curl: 200 on /, /journey/journey.js, /journey/route.js, all chapter folders, /static/, /content/content.js, /tools/capture.py; all three old URLs serve redirect stubs. (f) zero console errors on every fresh load + full rides. **Pre-existing bug found (NOT M4 — proven identical on a served worktree of M3 `35a8e69`):** nav direct-jump from the p=1 end-hold to Mission leaves the camera stuck (~(-15.9, 16.3, 2.6), fov 44.2) — the camBlend's per-frame lift accumulates and its last write lands AFTER the director's one-shot hero-pose restore at p=0, with nothing re-writing the camera afterwards. State/route/checksum unaffected; scrolling recovers. Filed for M5 (frame-loop / camera-handback debt). Landed as four commits, not pushed. |
| 2026-08-03 | **M3 spore housing executed** (15-merge-and-architecture.md §4 / §3's remainder): the same-particle takeover and the shed dimmer — the last two reach-ins driving the hero's 4,200 spores from outside — are now MODES OF THE SPORE SYSTEM in `organism/spores.js`, behind the driver seat. **What moved:** `journey-v6/chapters/inspire-takeover.js` (465 lines: hero-shadow delta tracking, staged split/braid path port, conversion stagger, core cohort, knot-pearl cadence, ?tkdbg probes) and `inspire-ambient.js` (154 lines: capsule/global/history dim + per-dot F = f·(1−cv)+pw exchange, byte-exact color restore) are DELETED; their math lives verbatim in spores.js as `steer()` (split/braid) and `dim()` (color mode) — same constants, same formulas, same deterministic streams (seeds 9127/3187, same LCG family as the hero's own rand). The braid now reads the organism's OWN form language (ctx gains rimRad/rimYoff/LEAN_DIR/CAP_Y — anatomy.js's mirror stays chapter-side), and DRIFT_RX/RZ are derived from the shed's own BREEZE components in spores.js, so the D17 locus axis structurally cannot diverge from the drift. **Seat API (the seam rule — behaviour in the system, anatomy+intent in the chapter):** claim once `seat = sceneApi.spores.setDriver({ exits })` (exits = the chapter's EXITS anchors: az/riseMin/riseMax/knot per exit); per frame `seat.drive({ eff, time, matrixWorld, leanScale, transform, regions, globalK, grad })` — steer first, then the color pass reads the internal per-dot feed, one call, same frame slot as before (chapter animator runs after 'spore-drift'; ordering comments kept true). `seat.feed/active/n` are the QA surface (`_sporeSeat` on the chapter api; ?tkdbg unchanged). inspire.js never touches a buffer again. **Restore is structural:** ONE release path (`releaseSeat`) — positions hand back at the hero shadow, colors restore from the one base copy — reached by channels easing to ~0 in drive() (as before), by `setDriver(null)` (proven live: dimmed sum 6475.04126 at the rest → 7789.45663/3824844.72915 byte-exact on release, released handle inert), and by a one-frame watchdog in 'spore-drift' if a claimed driver goes silent. **Gates:** (a) p=0 checksum **7789.45663** (+ index-weighted 3824844.72915) exact at boot AND after 0→0.26→0.925→0 round trips at shipped T and at T=0.8; (b) perceptual parity vs pre-cut captures (capture.py 1440×900 + CDP p-stills): mission MAE 5.24 / inspire 7.61 / p=0.19 6.29–7.13, all inside the same-build noise brackets measured this session (mission 4.60–5.23, inspire 7.81–8.86, p019 3.95–6.30; p=0.19 stands in for "p≈0.20" because 0.20–0.245 is un-parkable by design); (c) zero console errors across the full ride 0→0.14→0.26→0.38→0.49→0.725→0.925→1.0→back, all 15 stops confirmed; (d) Hannah-sentence, machine half: six-leg scrub 0.19⇄0.30 sampled settled AND mid-glide — the effectively-visible Points census is IDENTICAL at every sample (13 populations, the 4,200-dot shed among them; nothing appears or disappears), conversion count oscillates 3271⇄4200 reversibly with reproducible cvSum, and the ?tkdbg order-probe held every frame sampled; (e) grep proof: no `groups.spores` attribute access anywhere in journey-v6/ (two remaining hits are identity-comparison exclusion guards in connect/final), both files deleted, inspire.js imports neither; (f) QA dial: plain load has NO readout DOM, NO [ ] listener, and ignores a planted localStorage T=0.9 (ships T=0.30); `?t=0.8` shows the persistent bottom-left readout ("TRANSFORM 0.80") and the visibly stronger organization, screenshots on record. **T baked = 0.30** — provenance recorded at the constant (`T_SHIPPED`, inspire.js): DECISIONS.md holds no chosen T (the dial build ended "awaiting Hannah's ride + her chosen T"), so the shipped value is the dial build's code default, explicitly a restrained first offer, to be replaced by her recorded number. Shipped path reads neither `?t` nor localStorage; persisting works only inside QA mode. **Standing caveat: the Hannah-sentence test is hers — a machine can prove conservation and parity, not perception; her own ride at the Inspire rest remains the human check before M3 is called accepted.** Landed as one commit, not pushed. |
| 2026-08-04 | **M5 debt burn-down — structural items executed** (15-merge-and-architecture.md §4, six commits, lowest-risk-first, each landing green; the a11y items, flag/dial registries and chapter audits belong to other M5 agents). **Checklist:** ☑ **flight machinery** `5b9f343` — the item's own check ("confirm by grep that nothing live references it") FAILED: ui-footer.js reveal() calls `journey.flyTo(1)` so the footer *rises* through its reveal band ("flies, never jumps" is that control's documented W4-E behaviour), so full deletion would change shipped behaviour. Deleted what IS dead (`flyToChapter`, zero callers), corrected the false "flight system is now unused by every path" comment, documented the sole caller at flyTo. Gates: zero errors, round-trip checksum exact, cue flight 0.925→1.0 lands p=1. ☑ **key-routing edge cases** `0843cae` — one rule across four raw-listener seams: meta/ctrl/alt chords and IME composition are never journey input (Cmd+ArrowDown=End, Alt+Arrow=history, Ctrl+Space, Cmd+G find-next, Cmd+[/]=Back/Forward all previously claimed); scroll.js onKey bail + [g] gains modifier/text-entry guard + QA [ ] dial modifier guard + state.js flight-cancel mirror; Shift+Space deliberately keeps the shipped forward mapping (documented). Verified live: plain keys travel, chords don't, Cmd+G no longer toggles the lens. ☑ **frame-loop error isolation** `5d7f17b` — organism animate() runs each animator in its own try/catch (throw ⇒ log ONCE by name, unregister, rest of frame + render continue; before, one throw skipped every later animator AND the composer render every frame); journey.js guards its spine-internal calls (director/seams/per-chapter drive()/snap()/lens/ui) with the same latch-once-and-disable. Verified by runtime injection (throwing 'journey-connect' animator + throwing inspire.drive — each logged once, disabled, full ride kept working; injections not committed). ☑ **stuck-camera fix** `6c03585` — **root cause in one sentence: the direct-jump camBlend reads its destination pose live from the camera, and at p=0 the director's hero restore is a one-shot, so on every later blend frame the camera still held the blend's own previous output — the blend lerped toward itself and converged near the jump's start pose plus the lift arc.** Fix: director exposes `applyHeroPose()` (the exact restore write, now also used by setOwned(false)) and the blend re-asserts it each frame while un-owned — destination writer first, blend on top, blend ends ON the restore (composition order kept; at f=1 the lift is sin(π)=0). Reproduced pre-fix (stuck at (-15.75, 16.39, 2.58) fov 44.23); post-fix the same reproduction lands the fresh-load hero pose exactly (posDelta 0, fovDelta 0, screenshots on record); all 20 directed nav pairs + end-hold→each of the 5 land within 9.8e-4 of placed-at-rest references; checksum exact after the matrix. ☑ **?capture=<p> freeze** `3badf8b` — one mechanism: the frame loop's shared clock is the ONLY time source animators see, so `sceneApi.freezeTime(0)` latches t=0/dt=0 for every time-driven system (dt=0 is already the deep-link "place, don't advance" convention) and taaFrame() holds one Halton sample so the accumulation converges to a single exact image. ?capture implies nointro; journey places at exactly p (chapter ids accepted = that chapter's rest, so capture.py can keep speaking pose names). Verified: two CDP shutters 5 s apart at ?capture=0.26, ?capture=0.6 (non-rest) and ?capture=inspire each PIXEL-IDENTICAL (diff bbox None @1440×900); normal load unaffected. capture.py's own TODO (POSE_QUERY + CHECK_IS_ADVISORY flip) left for the tier owner — flipping it honestly requires re-shooting goldens. ☑ **input policy (shield retired) + ☑ index.html→shell** `93683d4` (combined; entangled) — organism owns input as API: `setInputPolicy('journey')` turns off user orbit/zoom/pan at the source (OrbitControls enableRotate/Zoom/Pan; handlers verified to bail before claiming gestures for mouse/wheel/touch), taps reach the organism's own handler natively (same ≤400 ms/≤7 px numbers the shield replayed); deliberately not controls.enabled (that is the director's camera-ownership toggle — orthogonal). Shell: **index.html 1,513 → 80 lines** (head + markup + ONE module); main.js (280) carries both former inline scripts in execution order; the fast-forward's clock-skew is now `organism/intro.js accelerate({totalMs})` (page wires triggers only); styles verbatim into hero.css (575; hero composition + ENTRY + intro-fast classes) and journey/site.css (568; journey DOM + journey-cursor rules), link order preserved. Verified live: wheel over canvas travels the JOURNEY (no zoom), drag orbit delta 0.000000, real stem click fires the tap ripple (uPulseT reset from parked 1000), nav links navigate, ?free=1 orbits (13.7-unit drag) with grab cursor; intro plays identically, ?introat=0.55 freezes, fast-forward verified by CDP 1.1 s into the intro (skew = exactly the remaining 7,383 ms, intro-fast synchronous, journey boots immediately). **Gates every commit:** fresh load zero console errors; hero renders; ?nosnap=1 scrub 0→1→0 (36 stops) clean; p=0 checksum **7789.45663** (+ 3824844.72915) exact after every round trip. **Perceptual (after the shell move, the risky one):** same-session pre-M5 (served M4-tip worktree :8138) vs post-M5: mission MAE 4.55 (< 4.65–5.23 recorded floors), inspire 5.51 vs a same-session same-build floor of 8.29 — both below noise. NOTE: `capture.py --check` vs the on-disk goldens reads inspire 22.21, but those goldens are dated 2026-08-02 — before the D16 restage + spore unification changed the Inspire rest — a pre-existing stale-golden condition; re-shoot when ?capture= gets wired into capture.py. Landed as six commits, not pushed. |
| 2026-08-04 | **M5 debt burn-down — registries + accessibility executed** (15-merge-and-architecture.md §4 / M5 row, three commits, each landing green — the structural checklist above belongs to a parallel M5 agent). **☑ Flag registry** `1f00e31` — new `glowshroom/flags.js`, sitting at the glowshroom/ root (above organism/ and journey/, so either can import it without depending on the other) replaces eight independent `location.search`/`URLSearchParams`/`localStorage` parse sites with one module that parses once at load and exports named, documented accessors. **Inventory (flag → owner file(s) before → effect → QA/shipped):** `?capture=<p\|chapterId>` → main.js + journey.js → freeze the organism's clock at t=0, skip intro, place the journey at exactly p or a chapter's rest, for capture.py stills → QA/tooling. `?nointro` → main.js → skip the entry choreography (blank-value quirk preserved: `?nointro` alone is falsy, needs `?nointro=1`) → QA. `?introat=<p>` → main.js + organism/intro.js → freeze the entry choreography (CSS half + organism draw progress) at p → QA. `?hl=<region>` → main.js → force a hero region's highlight at boot → QA. `?lit` → main.js → force every callout into its hover state (same blank-value quirk as nointro) → QA. `?body=serif` → main.js → serif body-copy A/B variant → QA. `?free` → main.js → keep the hero's fully interactive orbit camera instead of handing input to the journey → QA/demo escape hatch. `?nosnap=1` → journey/scroll.js → disable commit-resolution snap, for `?p=` deep-scrub QA → QA. `?aspect=portrait\|landscape\|<n>` → journey/director.js → force the camera composition's aspect ratio → QA/capture. `?steady=1` → journey/journey.js → kill the handheld camera layer for reproducible pose sampling → QA. `?p=<0..1>` → journey/journey.js → deep-link to a raw progress value → QA/deep-link. `?pose=<chapterId>` → journey/journey.js → deep-link to a chapter's rest progress, writes the route → QA/deep-link. `?notaa`, `?nofade`, `?dbg` → organism/organism.js → disable the TAA pass / disable the coverage fade / print live motion values → QA (all three are **substring matches** on raw `location.search`, not parsed keys — reproduced exactly, flagged in-file for a future tightening pass). `?tkdbg` → organism/spores.js → perf + animator-order probe hooks on the spore driver seat (writes `window.__tkPerf`/`__tkLast0`) → QA, same substring-match note. `?t[=<0..1>]` → journey/chapters/inspire/index.js → activates the TRANSFORM dial's QA mode → QA (now owned jointly by flags.js's `T_QA_ACTIVE`/`T_QS_VALUE` exports and journey/dial.js, see below). **Storage:** `journey-v6.transform` → `journey.transform`, migrated once at module load (old key read and copied forward only if the new key is unset, then the old key removed either way; wrapped try/catch, silent no-op in privacy mode). Not migrated (out of scope, not a flag): the `[g]` raw/finished-grade keyboard shortcut in journey.js — it never reads `location.search` or `localStorage`, nothing to move. **☑ Taste-dial registry** `5f68cd2` — new `journey/dial.js`: `createDial({name, shipped, min=0, max=1, step=0.05, keys=['[',']'], format, active, qsValue, loadStored, saveStored, onChange}) -> {value, active, set(v, {persist})}`, owning the shipped-default/QA-activated/clamped-to-a-step-grid/key-adjustable/read-out/persisted-while-QA pattern. dial.js never touches `location.search` or `localStorage` itself (that would make it a second flag parser) — call sites pass their activation/value/storage in from flags.js, keeping flags.js the sole parser. TRANSFORM is re-implemented on top of it in chapters/inspire/index.js with identical observable behaviour (T_SHIPPED 0.30, T_STEP 0.05, `[`/`]` keys, `.j-dial` readout markup restyled from `.j-tdial` 1:1) — its local `qaDial`/`tState`/`showDial`/`clampT` machinery is gone, replaced by one `createDial()` call plus `tDial.value`/`tDial.set()`. Registry is the deliverable; TRANSFORM remains its one client — no dials added for handheld amplitude or commit blend yet, per doc rule 6, each is now a one-line `createDial()` call away. **☑ Accessibility** `982be54` — skip link (`.skip-link`, "Skip to content") as the first focusable element, jumping keyboard visitors past the decorative canvas and its three floating HUD callouts to `<nav id="site-nav" tabindex="-1">`. **Router collision found and fixed:** journey/state.js's hashchange handler (`parseHash`) treats any hash it doesn't recognise as an unknown chapter route and rewrites it to `#/mission` — so the skip link's native `href="#site-nav"` fragment jump was getting stomped mid-flight by the router before focus landed. Fixed in main.js: the click handler calls `preventDefault()` and moves focus to the target element directly, never letting the browser touch `location.hash` (the `href` stays real markup as a no-JS fallback). `:focus-visible` added in hero.css, gold family (`var(--gold-bright)`), matching the treatment journey/site.css already carries for every journey-layer control (nav a covering .logo/2RP/Discord; `.cta` gets a second, larger, brighter ring since it already carries a permanent decorative outline that a plain focus-visible override would be invisible against). `#stage` gets `role="img"` + a descriptive `aria-label` (the canvas is decorative-interactive, so a single label is the honest amount of ARIA — no other roles or live regions added). Tab order confirmed sane downstream of the callouts (pre-existing HUD elements, unchanged, out of this item's scope): skip-link → co-connect → co-inspire → co-equip → nav logo → 2RP → Discord → CTA, each with a visible gold ring (screenshots on record); footer unaffected (10 links reachable at p=1, not hidden/inert). **Gates (all three commits):** fresh load zero console errors; every flag verified live with its expected effect (nosnap scrub, free orbit, `?t=0.8` readout + `[`/`]` step to 0.85 + persist to `journey.transform`, `?capture=0.26` freeze verified pixel-stable across two samples, `?introat=0.55` freezes all 27 entry animations, `?nointro` blank-value non-skip vs `?nointro=1` skip both confirmed, `?aspect=portrait` reframes fov 54.3→64 at the Owned rest, `?tkdbg` populates the perf probes, `?p=0.49` lands in Connect, `?pose=inspire` lands at the Inspire rest with the matching hash, `?steady=1` kills handheld jitter (position identical across two samples at fixed p; default build does not), `?hl`/`?lit`/`?body=serif` toggle visually, `?notaa` disables the TemporalAccumulatePass, `?nofade` zeroes `uFadeOn` across all 5 materials, `?dbg` injects the debug readout div); storage migration verified (planted `journey-v6.transform=0.63` → reload with bare `?t` → `journey.transform` holds 0.63, old key gone, T reads 0.63; shipped load with a planted `journey.transform=0.9` still ships T=0.30 with no readout DOM); p=0 checksum **7789.45663** exact at boot and after a 0→0.26→0.925→0 round trip. **Two items logged, not code (doc rule, deliberately left as-is):** (a) `assets/test-portraits/` is still present BY DESIGN — its own README already says LOOK-DEV ONLY / delete before launch, and the consent rule (13-content-ops.md) requires deletion before any PUBLIC deploy, which has not happened; it remains a pre-deploy checklist item, not a bug to fix now. (b) D11 (reduced-motion → static-tier automatic routing) remains parked awaiting Hannah's decision — the static/ Tier-3 build exists, is complete, and is linked from the footer's "the static journey" note (a link a person chooses to follow); only the *automatic* routing on `prefers-reduced-motion` is undecided, deliberately not wired per ui-footer.js's own comment. Landed as three commits, not pushed. |
| 2026-08-04 | **M5 debt burn-down — NO-SELF-IGNITION + LOCUS audit of Connect/Owned/Final executed** (15-merge-and-architecture.md §4 / M5 row; rules 4 (D16) and 5 (D17) of §1 as criteria; occluded streaming exempt, "anything visible when it arms is not"; end-hold 0.925–1.0 audited as Final's departure; Inspire DONE per its restage, Mission pre-lit by definition — neither re-audited). **Method:** full code inventory of every luminous element + reveal mechanism per chapter, then live verification at localhost:8137 with ?nosnap=1 — a wheel-event parking rig to hold arbitrary p against the soft snap, `freezeTime(0)` for pixel-stable frames, ~30 stepped screenshots across the arrival/departure bands (connect 0.39–0.49, owned 0.615–0.725, final 0.79–0.90, end-hold 0.925–1.0, plus reverse rides), and frozen A/B frames with each chapter's group collapsed to measure exactly what it contributes at the arming pose. **INVENTORY — Connect** (colonnade/blades+veins+beads+spores+haze, ADOS strands+knot, Hivemind braid+memory points, hero-bokeh dim, exit pulse): whole chamber fades on uAmount when the p-hold arms at 0.405 — frozen A/B at 0.415/0.425 proved the chamber contributes NOTHING visible from outside (the hero cap's occlusion shells genuinely occlude; the under-rim texture is the hero's own pre-lit gill fan) → arrival fade = occluded streaming, LEGAL; hover/ambient pulses, knot warm-up, memory points lit by the passing head, camera-driven exit pulse = brightenings of already-lit geometry, LEGAL; hero-point dim/retire with byte-exact restore = dimming, LEGAL; **VIOLATION (b): reverse re-arm** — the T2 floor predicate dropped/re-armed the chamber at camera y≈0.65 in OPEN AIR above the soil, so scrubbing backward faded the blade tissue in from nothing on screen → FIXED `859c965`: CONNECT_HOLD_HI 0.63→0.705, retire/re-arm now inside the soil-crossing murk. **INVENTORY — Owned** (substrate cords/hyphae/agg layers/lid, 48-portrait field, growth-front fan, DOM-hosted claim pulses, wavePulse/surge): **VIOLATION (b), the big one: arrival** — armed at p 0.63 with the camera ~1.5 units ABOVE the soil; the soil does not occlude (additive), so the ENTIRE colony incl. portrait faces time-faded in over the bare descent gaze (pre-arm frame at 0.615 = near-black soil; steady frame at 0.665 = full field; both directions violate) → FIXED `ede98a8`: all chapter fades scaled by amount×arrival, a p-mask (0.692–0.712) completing exactly inside the lid-crossing murk (verified: bare descent at 0.672, colony resolving through material at 0.706, full pre-lit field at 0.716; reverse ride retires it behind the same murk); **VIOLATION (b): growth-front fan** — whole-strand opacity fade-in 0.775–0.81 straight down the rest gaze (perceptually buried in the field but mechanically the banned primitive) → FIXED same commit: opt-in `growGate` shader variant (uGrow, feathered tip) draws each strand on from its aAlong=0 root in the lit deep field — growth from lit geometry; NOTE: a first cut added the gate to the SHARED makeFadePulseMat and shifted the owned rest's top band ~+4 MAE (isolated by stash/variant captures) — rejected for the opt-in injection, which restored the rest to the noise floor; **VIOLATION (b): end-hold retire** — OWNED_HOLD_HI 0.97 faded the colony's cutaway-wedge glow out in view during the end-hold (and re-ignited it backward; frozen A/B at 0.96 shows the contribution is a faint lower-left glow — sub-perceptual but mechanically illegal) → FIXED same commit: hold covers the whole tail (1.01); wavePulse/surge/claim pulses = brightenings of lit nodes, LEGAL. **INVENTORY — Final** (soil slab+cut face, surface strokes, lip, colony-in-section, connectors, ring members+primordia, spore sky, mist/horizon, uPull kindle, front pulse, CTA wave, heroDim): uPull undarken = camera-driven kindling from the hero around the arc, in place, re-darkens in reverse — LEGAL (D12's approved mechanism); primordia = dwell-gated growth from the lit colony, drains in reverse — LEGAL; front/CTA pulses = brightenings of lit geometry, LEGAL; heroDim = dim with exact restore, LEGAL; **VIOLATION (b): slab pop** — the opaque occluder appeared with group.visible the frame the chapter armed at 0.845 (camera 0.17 under the soil): steady frames at 0.843 vs 0.847 showed most of the visible Owned colony BLACK OUT in one frame (and re-ignite instantly on a reverse ride ~2.5 s after release) → FIXED `55629cf`: hashed-alpha dissolve (screen-space hash vs uSoilOn, honest depth writes, TAA integrates the stipple; uSoilOn=1 = pixel-identical solid) — occlusion now builds like fog thickening, in place, symmetric in reverse; **VIOLATION (b): post-surface fade** — amount kept time-fading the whisper bases/haze/mist/sky in view after the camera pierced the surface (at slow scrub the world was still at ~40–80% when it became visible) → FIXED same commit: FINAL_HOLD 0.845→0.80 (arm deep in the colony, chapter dark) + camera-keyed rise mask (x −4.6→−7.4, pure in the pose like uPull) OR-combined with amount, so every fade saturates before the pierce at any scrub speed; the pierce itself is the natural material crossing; verified 0.825 (colony being swallowed by thickening soil), 0.843≈0.847 (pop gone, continuous approach glow), 0.856 (surfaced world complete, cutaway wedge reads per the approved still). End-hold band: scene pre-lit, only DOM footer chrome and legal primordia growth — CLEAN. **(c)-items parked: NONE needed re-choreography.** Two residuals documented, not fixed: (1) fast-fling compression — the arming clocks (k 2.2–3.0/s) can still be mid-ramp if a maximum-rate fling (MAX_SCRUB_RATE 0.45 p/s) outruns them (Connect under-rim entry ~0.1 s after arm; Owned/Final are now p/pose-masked and immune) — same accepted class as Inspire's "fast flights compress the handoff"; a p-keyed arrival mask for Connect is the cheap harden if Hannah ever sees it. (2) The Owned descent band 0.63–0.692 no longer previews the colony through the soil (it cannot, lawfully — no occluder); the approach is now bare hero root-web until the crossing murk. Flag both for Hannah's next ride. QA oddity noted for future capture work: under `freezeTime(0)` the Owned portrait field renders several stops dimmer in some arrival paths (time-keyed portrait channels at t=0) — affects frozen QA frames only, never the live ride. **Gates (all three commits):** fresh load zero console errors; full ride 0→1→0 (0.02 steps, both directions) zero errors; p=0 spore-color checksum **7789.45663** exact at boot and after every round trip; rest-frame captures vs pre-audit baselines (same-session, capture.py 1440×900): connect MAE 14.59 (floor 19.63), owned 4.06 (floor 3.99, observed same-build spread 3.88–5.04), final 5.05 (floor 4.65, spread 4.38–5.16) — all inside noise; ~30 band screenshots + 6 frozen A/B pairs on record in the session scratchpad (m5audit/). |
| 2026-08-04 | **M6 CI gate executed** (15-merge-and-architecture.md §4 M6 row: "CI hook: capture.py + regression check on every scene-touching commit"). **capture.py rewired to the M5 `?capture=` freeze:** `POSE_QUERY`/`build_url` now emit `?capture=<pose_id>` (pose ids are chapter ids, resolved by journey.js's own freeze/deep-link path) instead of the old live-scrub `?nointro=1&pose=<id>`; the pre-freeze path is kept behind `--live` (cheap: one query-param branch + a longer settle constant) as a sanity check only, never a golden source. Settle dropped from 2.5s to `SETTLE_S_FROZEN = 0.6s` in frozen mode — freezeTime(0) pins TAA to one held Halton sample rather than accumulating, so there's nothing left to wait for beyond one paint; `--live` keeps the original 2.5s. **`--check` is now a REAL gate:** `CHECK_IS_ADVISORY` collapses to `args.live` (frozen mode, the default, is never advisory); exit code 1 on any FAIL-band file or a missing golden. **Threshold, measured not assumed:** shot the golden list once (`python3 tools/capture.py`), then ran `capture.py --check` against those same goldens twice back to back — both runs reported MAE 0.00/255, 0.0% px>8 on all 10 golden/size pairs (5 poses x desktop/mobile), i.e. the frozen frame is bit-identical run to run, not just low-noise. `FROZEN_MEASURED_SPREAD = 0.0`; per the plan's own rule (3x observed spread, floor 1.0), `FAIL_MAE_FROZEN = max(3x0.0, 1.0) = 1.0`, `WARN_MAE_FROZEN = 0.5` — the floor governs, deliberately: a literally-zero measured spread must not produce a zero-tolerance gate that trips on cross-machine GPU-driver/font-hinting noise. A third `--check` was run after touching (not editing) `index.html`, `journey/journey.js`, `organism/organism.js` on disk (server has no-store headers already) — still MAE 0.00 everywhere, exit 0. **New goldens (all frozen, `?capture=` freeze, dpr 1, chrome hidden):** mission/inspire/connect/owned/final x desktop 1440x900 / mobile 430x932 — the same set and resolutions capture.py already defined, now shot at the merged, Hannah-era-approved tip (commit `dcdb445`, the M5 locus-audit head) instead of the pre-D16-restage 2026-08-02 build. `static/captures/manifest.json` records `"frozen": true`, `"commit": "dcdb44580cba6e0716d7616985e78ee3ed14ad1e"`, and `"reason": "pre-restage goldens retired; frozen-capture era begins (M6) — shot through ?capture= at the merged, Hannah-era-approved tip"` (overridable via `--note`). Threshold + spread table: FROZEN_MEASURED_SPREAD 0.0/255 (10/10 files, 2 back-to-back runs); FAIL_MAE_FROZEN 1.00/255; WARN_MAE_FROZEN 0.50/255 (LIVE mode, `--live`, keeps the old ADR D5 percentages — warn 1.275/255, fail 5.10/255 — unchanged, still advisory). **Induced-failure proof the gate actually gates:** temporarily changed `organism/organism.js`'s `C_MID` heat-palette color (`0xb96b1c` to `0x1c6bb9`, an in-canvas constant sampled across every chapter's rest) — `--check` failed correctly, all 10 files in FAIL-band (worst MAE 14.60/255 on connect mobile; real exit code 1, confirmed via a separate shell capturing `$?` since one run was piped through `grep`/`tail` for readability); reverted the edit, ran `--check` again — back to MAE 0.00/255 on all 10 files, exit 0. The perturbation was never committed (`git diff organism/organism.js` clean before staging). **Old (2026-08-02, pre-D16-restage) goldens moved to archive, not deleted:** `archive/captures-pre-D16-restage-2026-08-02/` holds the retired 10 PNGs + their manifest.json (recovered via `git show HEAD:...` before the working-tree copies were overwritten) plus a README explaining why they were retired (shot before the D16 Inspire restage `04a7d21`/`c098e23` and the spore-unification pass `ae1750e` changed the Inspire rest — the stale-golden false positive already flagged at M5, inspire MAE ~22 against them). **`tools/pre-commit` reviewed against the final capture.py interface and one real bug fixed before install:** the script was written as `python3 "$(dirname "$0")/capture.py" --check`, but it's installed as a SYMLINK at `.git/hooks/pre-commit` — when git execs a symlinked hook, `$0` is the symlink path (`.git/hooks/pre-commit`), not the resolved target, so `dirname "$0"` pointed at `.git/hooks/` and would have looked for a nonexistent `.git/hooks/capture.py`. Confirmed the bug directly (`bash -c 'echo $0; dirname "$0"' .git/hooks/pre-commit` printed `.git/hooks/pre-commit` / `.git/hooks`) before fixing. Fix: resolve `CAPTURE_PY` off `git rev-parse --show-toplevel` instead (git always execs hooks with cwd at the working-tree root, so this is symlink-proof), with an explicit existence check and error message if the layout ever moves. `--check`'s exit-code contract (0 pass / 1 fail, both now real in frozen mode) needed no other changes — the hook already treated any nonzero exit as failure. Made executable (`chmod +x`) and installed: `ln -sf ../../ados-paris/glowshroom/tools/pre-commit /Users/hannahomalley/nigel/.git/hooks/pre-commit` (symlink verified resolving to the real file; `file` reports it as an executable shell script through the link). **Scratch-commit proof the hook actually runs:** appended a trailing blank line to `journey/journey.js` (whitespace-only), staged it, ran `git commit` — the hook fired, resolved `capture.py` correctly through the symlink fix, ran the full frozen `--check` (all 10 files MAE 0.00, PASS), and the commit landed (`d93310a`); then `git reset --mixed HEAD~1` + `git checkout -- journey/journey.js` cleanly discarded the scratch whitespace change (verified via `git diff` — empty) while leaving the real M6 changes (new goldens, capture.py, pre-commit, archive/) untouched in the working tree. **Not independently verified:** hook behavior when the server is down (the `curl` pre-flight exits 1 with an instructional message — read, not executed live, since :8137 was up throughout this session) and behavior on a genuinely different machine (GPU-driver/font-hinting cross-machine noise is the reason the floor exists, but this session only has the one machine to test on). |
