# Execution Plan — Subagent Breakdown (decision D9)

All work is delivered by Claude subagents orchestrated from the main session.
**Model policy:** `fable` = deep taste / visual sense · `opus` = medium-to-difficult engineering · `sonnet` = easy/mechanical.
**Testing:** every visual and performance test runs **on this machine** (the Mac serving `http://localhost:8137`) — decision D8.
**Motion:** designed by fable agents, then **reviewed live with Hannah** before it's locked.
**Guardrail for every agent:** never modify `golden-mushroom-page.html` or `mushroom-scene.js` — the running hero stays frozen; all build work happens in `glowshroom/journey-v6/` (dev copy) until final promotion.

## Wave 1 — launched immediately (parallel)

| ID | Task | Model | Output |
|---|---|---|---|
| W1-A | Baseline freeze: archive hero files, capture screenshots (all viewport modes + `?introat` intro points + callout states), measure bundle/TTI/frame-time, write BASELINE.md with regression budgets | opus | `BASELINE.md`, `archive/`, `baseline-captures/` |
| W1-B | Architecture ADRs (D2–D5) + dev scaffold: harvest map of `journey/` donor modules, world-layout ADR, `glowshroom/journey-v6/` scaffold that renders pixel-identical to the hero | opus | `adr/`, `glowshroom/journey-v6/` |
| W1-C | Anatomy & camera map (two pages, per `03`) drawn from the live hero's actual geometry + the approved stills | **fable** | `map/page1-anatomy.*`, `map/page2-camera.*` |
| W1-D | Placeholder content package: content module (anonymous contributors, PLACEHOLDER links, spotlight/card/profile stub copy per the locked copy table), no live modules | sonnet | `content/` |

## Wave 2 — after W1-B + W1-C (parallel)

| ID | Task | Model | Gate |
|---|---|---|---|
| W2-A | Spike A: extension lens — rear-cap geometry, orbit rough, plume treatment v1, dense Connect frame, optics stack + raw/finished toggle, budget table | **fable** | Review with Hannah |
| W2-B | Spike B: Owned portrait treatment at production density (placeholder faces), anonymous fallback node styling | **fable** | Review with Hannah |

## Wave 3 — after W2-A

| ID | Task | Model | Gate |
|---|---|---|---|
| W3-A | Grey-box prototype: full path Mission→Final, journey state, scroll scrub, thresholds, reverse-scroll, deep links | opus | — |
| W3-B | Motion pass on the grey-box: orbit spline, easing families, rest bands, text pinning, snap feel | **fable** | **Live motion review with Hannah on this machine** |

## Wave 4 — after W3 acceptance (parallel lanes)

| ID | Task | Model |
|---|---|---|
| W4-A | Chapter: Inspire (production) | **fable** |
| W4-B | Chapter: Connect (production) | **fable** |
| W4-C | Chapter: Owned (production, placeholder people) | **fable** |
| W4-D | Chapter: Final pullback (production) | **fable** |
| W4-E | Product systems: nav, routing, deep links, drawers/cards DOM, state torture-tests | opus |
| W4-F | Platforms: portrait poses wiring, touch model, bottom sheets, tiers + watchdog, reduced-motion static journey, Tier-3 capture script | opus |

## Wave 5 — hardening

| ID | Task | Model |
|---|---|---|
| W5-A | Full QA sweep vs `14-qa-acceptance.md` (25 criteria) on this machine, incl. emulated Tier 2 | opus |
| W5-B | Fix rounds (model chosen per defect class) | per-issue |
| W5-C | Decorative-motion cull pass per chapter (PS-4.4) | **fable** |
| W5-D | Content merge when real data arrives (replaces placeholders) | sonnet |
| W5-E | Final review with Hannah → promote `journey-v6/` to the live page | — |

## Status log

| When | Event |
|---|---|
| 2026-08-02 | Wave 1 launched (W1-A/B/C/D) |
| 2026-08-02 | **W1-D complete** (sonnet): `journey-v6/content/` — five-chapter content module with locked copy verbatim, 16 anonymous contributors (consent:false), 54 greppable PLACEHOLDER markers, deferred Equip content archived separately, COPY-TABLE.md maps every placeholder to its supplier. No fake stats or live modules. |
| 2026-08-02 | **W1-A complete** (opus): BASELINE.md + regression-check.md written; hero archived (both files, byte-identical, hero untouched). Numbers: 1.49 MB raw / ~319 KB gz over 14 requests (three.module.js = 87.5%); TTI 340 ms warm; locked 60fps with ~1.2 ms CPU frame cost; 41 draw calls / 44k lines / 24k points / 4,200 spores. Machine: MacBook Air M3, 8 GB. Findings: **hero has NO WebGL-failure fallback**; reduced-motion only stops the intro (scene keeps animating); 01 INSPIRE label clips at right edge in desktop mode (baseline, do-not-silently-fix); tab order and focus styling gaps recorded. Gaps: no persisted PNGs (browser-harness limit — Playwright pass recommended), no cold-cache TTI, no CPU-throttled Tier-2 numbers. **G0 pending: budgets are proposed, awaiting Hannah sign-off.** Screenshot-diff gate must be structural (anchors ±2px + renderer.info) — per-pixel always fails due to breeze/TAA. |
| 2026-08-02 | **Hotfix (orchestrator):** `journey-v6/index.html` input shield — OrbitControls wheel-zoom, drag-orbit, and tap-pulse blocked at capture phase on the journey page (hero page untouched); ambient cursor wind kept; `?free=1` escape hatch restores hero interactions. Verified live: 25 synthetic wheel events → zero camera movement with shield, large zoom with `?free=1`. Scroll-drives-journey arrives with W3-A (grey-box). |
| 2026-08-02 | **W1-C complete** (fable): both map plates in `map/` (naturalist-plate style, verified in browser). Key calls: orbit around the RIGHT flank ~160° ending ~20° off dead-rear (follows the breeze drift +x; reveals the gill-exposed lifted rim earliest); exits: ArtCompute = back-right (the plume already visible in the hero), Arca = rear-centre tallest, 2RP = back-left — mirrors to the approved still's layout from the rear pose; ADOS = the hero's existing hot core at the gill/stem apex; Hivemind braid doubles as the Connect→Owned exit guide; Owned glide heads −X toward the fairy-ring growth front. Conflicts logged (mockup nav, owned.png group names vs locked claims, inspire.png symmetry). Awaiting sign-off note. **WAVE 1 COMPLETE.** |
| 2026-08-02 | **Gate G0 CLOSED:** Hannah approved the regression budgets (BASELINE.md §8 now binding: ~bundle +10%, TTI +10%, +1 ms frame time at the hero pose; structural screenshot gate). |
| 2026-08-02 | **Wave 2 launched:** W2-A extension-lens spike (fable) → `journey-v6/spike-a/`; W2-B portrait-field spike (fable) → `journey-v6/spike-b/`. Both end in review sessions with Hannah. |
| 2026-08-02 | **W2-A complete** (fable): Spike A live at `localhost:8137/journey-v6/spike-a/`. Grade pass inserted into the hero's own composer (raw = the approved hero exactly; graded = +1 draw call only); 5,100 GPU spores born between real gill channels, rim curl, braided three-core plumes leaning with the breeze; orbit spline hero→Inspire rest with sequential exit reveal; dense Connect colonnade frame; anamorphic streak on one active exit. Budgets in BUDGETS.md — every state vsync-locked 60fps; hero-raw row exactly matches the frozen baseline. Self-flagged gaps: plume-core definition ~70% of the still, hero ambient shed competes with ArtCompute plume (production fix per ADR D3), Connect frame looser than the still, motion needs one visible-tab confirmation, reduced-motion grain freeze not wired. **Awaiting Hannah review (G2a).** |
| 2026-08-02 | **W3-A grey-box complete** (opus): full five-chapter scrollable journey live on `journey-v6/index.html`. Virtual scroll (24vh, ~24s traverse), PCHIP scroll→p (max gain step 2.1%), soft snap (measured settle), 0.45 p/s scrub cap; 31-point forward+reverse audit: 0 mismatches, never inside the stipe, nav=owned through Final, zero errors, 60fps at rests. Reused Spike A orbit; found+fixed 4 real bugs (T2 seam predicate needs floor clause — ADR amendment; flight-cancel camera disagreement; deep-link Back off-page; spike-a setLeanScale TypeError) + 2 motion defects (145°/s orbit whip → trapezoid; Final 180° whip-pan). Acceptance: all GB items pass except Final visually thin, Owned node reachability, label crowding, portrait poses formulaic — handed to W3-B/W4. GREYBOX-DECISIONS.md written. |
| 2026-08-02 | **Orchestrator:** spike-a setLeanScale guard applied (same unfinished-revision class as the labels/dust fix). |
| 2026-08-02 | **W3-B motion pass launched** (fable): handheld documentary layer (seeded, ?steady=1 QA), orbit breathe, Final rise-tilt-recede as one gesture + fog reveal timing, seam pass-through feel, copy fade choreography, reverse-scroll pacing, Inspire label stagger. Strict file boundary vs parallel W4-F. Ends with Hannah's ride-through script. |
| 2026-08-02 | **W4-F (first half) launched** (opus): `tools/capture.py` Tier-3 capture pipeline + complete static journey at `journey-v6/static/` + TIER-WIRING.md integration spec. New files only — no core conflicts. |
| 2026-08-02 | **GATE G3 APPROVED WITH NOTES (Hannah, live ride-through):** motion approved ("moving really nicely"; Owned + Final motion "feel really nice"). Binding note: eliminate no-man's-land — idle must always resolve to a chapter rest, forward-biased. Connect gills confirmed biologically accurate (anastomosing lamellae); production must read as blade-tissue not fabric. Wave 4 green-lit ("keep working through it"). |
| 2026-08-02 | **Wave 4 launched (batch 1 of 2, strict boundaries):** snap-commit scroll (fable — core/scroll.js+constants only); W4-D Final production (fable — chapters/final*, the empty stage: ring, cutaway, spore sky, sequential activation); W4-C Owned production (fable — chapters/owned*, Spike B treatment at the real leg, placement rules, ≥10 reachable nodes). Batch 2 after these land: W4-B Connect (blade-tissue gills), W4-A Inspire polish, grade unification across chapters, W4-E systems hardening. |
| 2026-08-02 | **Touch/bottom-sheets complete** (opus): input-ownership registry replaces selector guards (claimInput/releaseInput, modal mode kills travel keys); controls-first key dispatch by platform semantics; first-tap-arms/second-tap-opens per live pointerType; drag-dismiss sheets (28% or flick), journey p provably frozen during sheet interaction; 44px audit 21/21; closeCard fade restored; aria-expanded + live-region retarget announcements; footer internal scroll was dead — fixed via non-modal ownership-while-overflowing. Footer-peek report = hidden-pane vh-collapse artifact (hardened anyway). **Flagged for Hannah: mobile nav band moved to its own centred row (hero row overflowed 375px — Owned + CTAs were unreachable); needs design eyeball.** Chapter setSelected contract task launched (opus). |
| 2026-08-02 | **D15 ring hero-density rebuild complete** (fable): 825 → 21,280 line segs + 2,001 pts across the ring (near members ~24% of hero build each, mirrored technique: cap lattice+overlay net+motes, feathered gill fans w/ cavity curve, 3-ring rim + front arc, stem lattice+fibres, forking ground-merge roots; far-side damping replaces occlusion shells at zero draws); per-system opacity via build-time color mul in the shared batch — **draw calls identical (104), frame cost unchanged (vsync-locked)**. Same-render verdict: pass — one material world at rest and mid-reveal; characters preserved; whisper state carries density. Tier-2 cut order recorded. (Nav showing OWNED at epilogue = designed behaviour per handoff, not a bug.) |
| 2026-08-02 | **setSelected contract complete** (opus): owned.js routes contributors via the bridge's exact mapping (guarded release) + pods get a held sel channel (0.55, no claim pulse); inspire.js holds the selected exit's coherence + streak priority hover→selection→auto, alias-safe; bridge retirement proven empirically (stub test); 10/10 verification paths green. Connect owes the method (noted §24); Final exempt by design. Duplicate :8137 server the agent spawned was killed by orchestrator; original intact. |
| 2026-08-02 | **Hannah review of ring: NOT same-render — "way worse texture."** Root cause: D14 carried form at ~1/50th hero density (825 segs total vs hero's 44k). **Hero-density rebuild launched** (fable): near members 30–40% hero density each, mid 15–20%, far 8%, texture systems replicated (cap lattice+points, dense gill skirt, stem fibres, ground-merge, glints); batching kept; frame-cost measurement required. |
| 2026-08-02 | **D14 ring variants complete** (fable): ring members rebuilt as parameterized instances of the hero's actual form recipe (rim harmonics, droop+fold, rolled margin, cap lumps, soft-bell dome, gill cavity curve w/ vein gills, stem taper/ground-merge flare, heat palette) with seeded per-member individuation — nine distinct characters (patriarch m1, broad-brimmed elder m5, button m8 closing the ring beside the hero, etc.), per-member shed strength weighting the sky sources with zero sky edits. Draw calls unchanged. Same-species verdict: genuine; caveat: two near short members read ~15% more open from above until the grade adds density. Undarken/reverse/primordia intact; hero at p=0 pixel-identical. |
| 2026-08-02 | **Portrait framing complete** (fable): authored per-rest offset fields (dolly/rise/truck/re-aim/fov) smoothstep-blended over poseAt by aspect (zero at ≥1, full at ≤0.75), guard keys pin the field near zero in the tight slip-under/descent clearances; poseAt stays pure with aspect as a param; landscape bit-identical (20-sample 10-decimal diff clean); all five portrait rests verified at 430×932 + 375×812 vs map thumbnails; ?aspect=portrait QA. Flagged: hivemind anchor unholdable in portrait (relayed to Connect agent); footer cue peeks at 375×812 Final rest (relayed to touch agent). |
| 2026-08-02 | **W4-E footer+a11y complete** (opus): progress-driven footer (reveal band 0.955–0.992, above the commit point so it's never half-parked; SITE INFORMATION cue flies to p=1 and hands focus); roving tab order finished incl. two invisible-but-focusable leaks fixed (.j-nav at hero, callout tags off-chapter); nav is a labelled landmark; cards are true aria-modal dialogs with trap+return; focus styling = hover parity; Space-key conflict guarded (root cause in scroll.js catalogued); selection hook added (openCard/closeCard → chapter setSelected, bridged to owned-portraits' orphaned implementation). One introduced regression (bare nav{} rule leak) caught and fixed. Six-item pre-existing a11y debt list catalogued. |
| 2026-08-02 | **Touch/bottom-sheet agent launched** (opus): first-tap-hover/second-tap-open, bottom sheets w/ drag dismiss + working internal scroll, 44px targets, a11y debt items 1/4/5 root-caused. Boundary: ui*, scroll.js input routing only, index.html journey block. |
| 2026-08-02 | **W4-D Final production complete** (fable): D12 implemented — ring centred at (−6,−0.8) so the hero sits ON the arc (member, not parent, ~11° right of centre at rest); undarken reveal = monotonic camera-driven kindling CCW from the hero, reverse re-darkens; camera re-keyed within the approved gesture family (yaw 833–970°/p, single pitch crest, arrival=recede vector); growth-front pulse 13–18s revolutions; primordia on ≥6s dwell; CTA hooks wired (incl. footer delegation). Zero errors; narrow-window verified. Honest gaps: p≈0.85 quietest frame; raw render less dense/luminous than the still (grade's job); hero ground network needs a clipping plane (logged). Budgets recorded; grade-pass notes: manual fog uniforms (don't double-fog), ring halo/core knobs. |
| 2026-08-02 | **Portrait-pose agent launched** (fable): authored per-chapter portrait compositions vs map thumbnails, aspect-blended offset field over poseAt, landscape bit-identical, ?aspect=portrait QA. Boundary: director.js integration + new core/portrait.js. |
| 2026-08-02 | **W4-A Inspire polish complete** (fable): core cohort + travelling knot pearls (Arca hottest per map) + 9 shader-driven core ribbons (+1 draw) bring plume definition to full vs the still (open taste knob: sheath width, flagged for grade pass); ArtCompute unswallowed via corridor-dim of the hero shed (restored verbatim on exit, verified numerically); streak auto-follows the igniting exit, Arca at rest; hover trace-back (plume→rim→gill origin) implemented on all three. Hero p=0 regression clean. |
| 2026-08-02 | **W4-C Owned production complete** (fable): Spike B treatment ported against the REAL leg (107-point polyline sampling); 48-node photo field, 7,400 hyphae + 6 cords + soil-underside lid so both crossings pass through material; pods + claim pulses; consent enforcement in code (48/48 forced anonymous with today's consent:false content); reachability fixed (13 hotspots live at rest, 16/16 contributors framed); 60fps, zero errors, Mission byte-clean. ACES compensation constants recorded for grade unification. **Flagged: parallel connect.js on disk has a GLSL reserved-word (`patch`) compile failure that blacks the composer when Connect arms — relayed to W4-B agent urgently. Also: ui.js lacks a selection hook (relayed to W4-E agent).** |
| 2026-08-02 | **Commit `5272da7`** (133 files) — local only: GitHub token on disk is DEAD (401) and no other credential exists on this machine (no gh, no keychain entry, no SSH keys). Push blocked until Hannah/Peter mint a fresh classic PAT (repo scope) into `~/.github_token`. Committing locally at each milestone meanwhile. |
| 2026-08-02 | **Snap-commit complete** (fable): no-man's-land eliminated — idle always resolves to a rest; COMMIT_THRESHOLD 0.35 (forward-biased), glide 0.10 p/s riding the normal scroll pipeline (copy/seams behave identically); p=1.0 added as terminal anchor; 12/12 settle tests + cancel-mid-glide + 30k px fling verified, zero errors. Feel dials for Hannah's next ride: threshold→0.30 for harder push, glide→0.12 if leisurely. `?nosnap=1` QA escape. |
| 2026-08-02 | **Orchestrator:** work snapshot tarball at `glowshroom/archive/snapshots/` (protection — the spike-a near-loss showed uncommitted work has no restore point; recommend a git commit when Hannah says go). |
| 2026-08-02 | **D12 relayed to W4-D mid-build:** hero joins the fairy ring; undarken reveal; scoped director.js Final-leg exception granted. **Batch 2 launched under D13 (keep moving always):** W4-B Connect production (fable — blade-tissue gills, three distinct behaviours, resolve community-vs-copy composition) + W4-A Inspire polish (fable — plume cores to full definition, unswallow ArtCompute, hover trace-back). Five agents now in flight, boundaries: scroll.js+constants | final*+director-final-keys | owned* | connect* | inspire*+anatomy. Remaining queue: grade unification, W4-E systems, mobile portrait poses, tier wiring (needs D11). |
| 2026-08-02 | **W3-B motion pass complete** (fable): handheld documentary layer (seeded, zero at rests, fades under fast scrub, ?steady=1 kills it); orbit breathe (±9% ease variation, azimuth still strictly monotone); Final re-keyed into one rise-tilt-recede gesture (yaw peak 1665→964°/p, fog staggered so the sky breathes open at the soil crest); T2/T3 seam fog-dips (pass-through feel, C1, closed before rests); copy release-on-travel / re-anchor-on-settle choreography; reverse byte-identical; Inspire labels moved to plume bodies with 150ms stagger + suppression hysteresis. Ride-through script delivered. Known Wave-4 items: Final stage nearly empty, optics stop at p 0.38, dips are fog-only stand-ins, handheld amplitude deliberately conservative (Hannah's knob). **BUILD FROZEN for Hannah's G3 ride-through — no agents touch journey-v6/ until her verdict.** |
| 2026-08-02 | **W4-F (first half) complete** (opus): `tools/capture.py` (CDP over stdlib WebSocket — ADR D5's --screenshot form hangs on this page; DPR 1 with measured --quantize option) captured all 10 stills clean (5 poses × desktop+mobile, 11.6 MB, none need the freeze to *look* right — freeze buys reproducibility); complete Tier-3 static journey at `journey-v6/static/` — zero canvas, zero errors, all content as authored HTML with `data-src` drift-guard (117 strings in sync), works with scripting disabled, AAA contrast over the stills, reduced-motion honoured, routes + focus management verified at both sizes. TIER-WIRING.md spec for the integrator (capability doors, ?capture= freeze list incl. captureReady flag, watchdog rules, gate hook). **Raised decision D11:** routing prefers-reduced-motion to static changes the accepted Mission path for those visitors — needs explicit ack. |
| 2026-08-02 | **Spike B rev 2 complete** (fable) — **G2b condition met, gate fully closed.** No-empty-frames audit passed at 19 scrub positions across two aspect ratios (fixed one real violation: node <1.5 units from path at u≈0.4 — now a 3.0-unit clearance rule); full Owned chrome as real DOM asserted from content.js (heading, primary+secondary pods with distinct colony-pulse hovers, wordmark, nav w/ Owned active, 2RP/Discord pair); camera reworked to a lateral arc (view 45–75° off motion vector, nothing streams at the lens) with frame-cell stratified node placement; boots into photos×48. 60–63fps, zero errors. Placement rules copied into 09-chapter-owned.md. Taste note for Hannah: the moving camera lives "inside the colony" so faces run larger than the still's wide vista — `?u=` scrubs for judging. |
| 2026-08-02 | **Gates G2a + G2b APPROVED (Hannah):** blanket approval — Spike A v1 is the lens baseline; Spike B approved conditional on rev 2 landing clean. Recorded in DECISIONS.md. |
| 2026-08-02 | **Wave 3 launched: W3-A grey-box prototype** (opus) — full scrollable five-chapter journey in `journey-v6/`: scroll model + soft rests, complete camera path with the four seams + fog re-parameterisation at rise, DOM copy layer + nav + flights, hash routes/deep links, interaction skeleton, hero preservation. Reuses Spike A's orbit/plumes for Mission→Inspire. W3-B motion pass follows; Hannah's live ride-through after that. |
| 2026-08-02 | **Spike A repaired (orchestrator):** the stopped v2 agent had left `director.js` expecting unbuilt `labels`/`dust` modules — every key handler threw. Fixed with inert defaults; verified live: all poses, key switching, and orbit playback work (orbit stall in headless test = known hidden-tab rAF artifact, advances when frames pump). Spike A is v1 behaviour, as Hannah directed. |
| 2026-08-02 | **Spike B rev 1 complete** (fable): [p] photo mode + [d] 48-node density live. Verdict: **photos beat the busts** — 48-node photo frame is closest yet to the approved still. Production findings: photos need ~2× colour push (62% desat + 0.90 amber multiply), background edge-burn is make-or-break (narrow working window → per-photo QA needed in production), wider mask feather; rim language unchanged. 48 nodes: 60fps, same draw-call count, voids intact; noted "row of coins" chains → size/spacing jitter rule for OW-4. |
| 2026-08-02 | **Spike B rev 2 launched** (fable, fresh agent — resume unavailable): Hannah's notes — no empty frames anywhere on the path (incl. vertical gaps), full Owned page copy/chrome (heading, claim pods w/ hover pulses, wordmark, nav w/ Owned active, 2RP/Discord pair), arc camera sweeping past faces (not head-on), decentralized composition, boot state = photos+48. |
| 2026-08-02 | **W2-B complete** (fable): Spike B live at `localhost:8137/journey-v6/spike-b/`. 9,900 flow-field hyphae + 5 rhizomorph cords, 16 portrait nodes (procedural busts) with 87 strand curves terminating exactly at nodes, real 3D rim fibres, frayed masks, defocus/haze depth; anonymous spore-print fallback; donor grade + raw toggle. 60fps sustained, 0.3–0.8ms CPU, zero errors; budgets appended. Self-verdict: embedded-not-pasted **passes at spike level**; caveats: painterly busts need re-tuning against real photos, 16 &lt; "dozens" density, close-hover runs hot, and a composer finding (runs WITHOUT OutputPass; sRGB-encoding olive-washes the additive field — must be reconciled with the hero composer at merge, flagged in BUDGETS.md). Keys: space glide, g raw, a anonymous, b HUD. |
| 2026-08-02 | **Spike B photo revision launched** (same fable agent, resumed): [p] procedural→photo→anonymous cycle using the staged test portraits; [d] 48-node production-density test; report on what real photos need to sit in the field. |
| 2026-08-02 | **Hannah direction for Spike B:** test with REAL photos, not only procedural placeholders. 26 free-license test portraits downloaded to `journey-v6/assets/test-portraits/` (20× 128px randomuser.me + 6× 512px pravatar.cc, README marks LOOK-DEV ONLY / delete before launch; consent rule unchanged). Photo-mode revision queued for when the running Spike B agent completes. |
| 2026-08-02 | ~~G2a review #1~~ **CORRECTION (Hannah): the "zooming at me / arc / sparse" review was about SPIKE B (the Owned field, watched mid-build in the shared browser pane), NOT Spike A.** Spike A is to be left exactly as delivered; the mistakenly-launched Spike A v2 agent was stopped mid-edit and Spike A verified still loading clean (zero console errors). Spike A's G2a review by Hannah has NOT happened yet. Her Spike B notes to fold into the next B revision: camera should move as an ARC (not things coming straight at the viewer), and the field must be less sparse / less centralized — distribute faces and structure through the whole volume instead of clustering in the centre. Queued behind the running photo/density revision. |
| 2026-08-02 | **Hotfix 2 (orchestrator):** tap/click restored on `journey-v6` per Hannah — the shield now replays a quick same-spot down+up through to the canvas (tap pulse works; zero drag delta so the camera can't move), while drag-orbit and wheel-zoom stay blocked. Verified via event trace: replayed pair reaches the canvas; drag and wheel storms leave the camera pixel-identical. |
| 2026-08-02 | **W1-B complete** (opus): six ADRs in `adr/` (D2–D5 + AR-6 resolved in DECISIONS.md); `journey-v6/` scaffold live at `localhost:8137/journey-v6/index.html` — console-clean, parity with the hero within run-to-run temporal noise (MAE 1.69–3.34 vs same-URL noise 2.79). Key findings: the hero needs **zero modification** (its `createScene()` already exposes `groups`/`consts`/`addAnimator`; extensions parent to `groups.mushroom` and inherit cap bend/sway); four occlusion seams replace veils; the hero's `Fog(bg, 7, 20)` clips past ~20 units → **the rise seam must re-parameterise fog before the Final pullback** (hard constraint on chapter 10); donor `equip.js` moved to `journey/deferred/`. |
