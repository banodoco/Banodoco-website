# Supplemental Tasklist — User-Authorized Sol Burst

The user explicitly authorized a temporary Sol implementation burst for obvious verified defects. This changed model assignment only; repository scope and no-push/no-deploy authority remained frozen.

| Task | Classification/model | Scope | Acceptance and evidence |
| --- | --- | --- | --- |
| SB-1 HTTP Range | normal, Sol by explicit override | `serve.py`, focused test | Correct closed/open/suffix/416/HEAD; reject multi; `.oracle/findings/wave3a/wave3a-L9-packaged-http.txt` |
| SB-2 Gate honesty | normal, Sol by explicit override | browser smoke, capture check, rebuild order, tests | No implicit green skip; readiness required; no repo check output; fresh captures before metadata |
| SB-3 Local intro clock | architectural/difficult, Sol | `main.js`, intro local clock, test | No mutation of `performance.now`; preserve ramp and frame ordering |
| SB-4 Static no-JS routes | normal, Sol by explicit override | static generator/output/test | Native chapter targets scroll/focus correctly without JS; enhanced aliases preserved |
| SB-5 Initial TAA sizing | normal, Sol by explicit override | initial composer sizing/test | DPR1/2 target parity before and after resize; no CSS-layout change |
| SB-6 Baked manifest boundary | normal, Sol by explicit override | loader/schema test | Validate before bin fan-out; preserve partial/per-chapter live fallback |
| SB-7 Suite integration | normal, Sol by explicit override | `package.json` | All new regressions run in authoritative `npm test`; existing tests preserved |

All tasks were implemented in disjoint lanes, locally validated, then independently reviewed. No task was committed by an implementer; host integration owns the checkpoint commit.
