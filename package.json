{
  "name": "banodoco-website",
  "version": "1.0.0",
  "description": "Banodoco Website",
  "main": "server/server.js",
  "scripts": {
    "start": "node server/server.js",
    "build": "cp -r src/js js && cp -r src/assets assets && cp -r src/styles styles && cp -r src/data data && cp -r src/pages pages",
    "dev": "npm run build && npm start"
  },
  "dependencies": {
    "express": "^4.18.2"
  },
  "devDependencies": {
    "nodemon": "^3.0.3"
  },
  "author": "",
  "license": "ISC"
} 