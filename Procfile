# Railway start command — serve the static tree (no build step).
# serve.py honors $PORT (Railway's env) and sends no-store + range headers
# (Chrome caches ES modules aggressively; no-store keeps deploys fresh).
web: python3 serve.py
